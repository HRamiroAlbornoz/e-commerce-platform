# Contexto del proyecto

E-commerce de periféricos gaming con dos roles (customer / admin). SPA en React + TypeScript,
Firebase como backend de autenticación y datos, AWS S3 para imágenes mediante presigned URLs
generadas por Vercel Functions, y Vercel para el deploy.

Este archivo complementa a `~/.claude/CLAUDE.md`; no lo reemplaza. Solo registra lo que es
específico de este proyecto.

## Excepción a las reglas globales

**El código de este proyecto no lleva comentarios.** Es la única regla que se aparta del
`CLAUDE.md` global, que pide comentarios en español en las funciones no obvias.

Consecuencias, para que la decisión no degrade el código:

- Los nombres cargan todo el peso. Sin un comentario que rescate un nombre flojo, las funciones se
  parten antes: partir es la única herramienta que queda para explicar.
- No se usan type assertions (`as Tipo`). La regla global las tolera solo con un comentario que
  las justifique; sin comentarios, la excepción se cierra. Si aparece una realmente inevitable, la
  justificación va al ADR o a la descripción del PR.
- La documentación **no** está afectada: `README.md`, `docs/spec.md`, `docs/arquitectura.md` y los
  ADRs se escriben completos, igual que en cualquier otro proyecto. Ahí viven los endpoints
  (método, URL, parámetros, respuesta, errores) y el porqué de cada decisión cara de revertir.

## El logger, con su condición

La regla general pide un logger estructurado como pino o winston. **En este proyecto no aplica
todavía, y el motivo está escrito para que se pueda revisar:** no hay un servidor de larga vida,
solo Vercel Functions, donde pino suma peso de bundle y tiempo de arranque en frío sin aportar lo
que lo hace valioso (transporte, rotación, destinos múltiples), porque Vercel ya captura y agrupa
la salida estándar.

Se emite un objeto JSON con `timestamp`, `level`, `requestId`, `code` y `message`. Lo que sigue
prohibido es lo que la regla realmente combate: un `console.log` suelto con un mensaje vago y sin
contexto.

**La condición:** el día que este proyecto tenga un backend propio de larga vida (un Express, un
servidor persistente), entra pino. No es una excepción permanente, es una regla con su condición
explicitada.

## Vulnerabilidades de npm audit — excepción monitoreada

`npm audit` reporta 5 (2 moderadas, 3 altas: `ajv`, `path-to-regexp`, `undici`), todas transitivas de
`@vercel/node@13.0.0`, que es la **última versión publicada** (no hay upgrade que las resuelva).
`npm audit fix --force` "arregla" bajando a `@vercel/node@4.0.0`: un retroceso de 9 versiones
mayores, no una solución.

**No se aplica el fix, y es deliberado, no un olvido.** `@vercel/node` se importa en `api/*.ts`
como `import type` — solo tipos, borrados en la compilación. El código real de ese paquete (donde
viven las dependencias vulnerables) nunca corre en la función desplegada: Vercel rutea con su
propia infraestructura de plataforma, no con esta copia de `node_modules`. Solo importaría si se
corriera `vercel dev` localmente, y ni así queda expuesto a internet.

Revisar de nuevo cuando Vercel publique una versión de `@vercel/node` que actualice esas
dependencias, o antes del Cierre si para entonces cambió el análisis.

### Segunda cadena: `firebase-tools`

Al sumar `firebase-tools` (paso 4 del andamiaje) aparecieron 7 vulnerabilidades más (todas
moderadas: `@google-cloud/pubsub`, `@opentelemetry/core`, `csv-parse`, `gaxios`, `stream-json`,
`uuid`, y `firebase-tools` mismo por arrastre). El único fix es `firebase-tools@10.1.1`, un
retroceso de 5 versiones mayores desde la 15.30.0 instalada — mismo patrón que `@vercel/node`.

**Tampoco se aplica, por un motivo distinto al de arriba pero igual de válido.**
`@vercel/node` no ejecuta en absoluto (import type); `firebase-tools` sí ejecuta, pero **solo en
tu maquina y en el runner de CI**, nunca en el código que se despliega — no es una dependencia de
`src/` ni de `api/`, no viaja en el bundle ni en la función. Las vulnerabilidades puntuales
(ReDoS en el parseo de un header "Baggage" de OpenTelemetry, un bug en `csv-parse` al importar
usuarios por CSV, un bounds-check de `uuid`) requieren que la herramienta reciba **input externo
adversarial**, y `firebase-tools` en este proyecto solo habla con el emulador local o con la propia
cuenta de Google — nunca con un tercero no confiable.

De las 3 vulnerabilidades **altas** que sigue reportando `npm audit`, las tres pertenecen a la
cadena de `@vercel/node` de arriba; ninguna es nueva. Total actual: 13 (10 moderadas, 3 altas).

Revisar de nuevo cuando `firebase-tools` publique una versión reciente con esas dependencias
actualizadas, o antes del Cierre.

## Datos operativos

| Dato | Valor |
|---|---|
| Repositorio remoto | https://github.com/HRamiroAlbornoz/e-commerce-platform |
| Repo local | Inicializado, rama `main` |
| Estrategia de ramas | Una rama por feature desde `main`, con las slices adentro. Tope de tres slices por rama |
| Merge | Siempre por pull request desde la interfaz de GitHub, con CI en verde. Nunca merge local. **Squash and merge**: un commit por PR en `main`, mensaje editado si el título del PR no alcanza |
| Formato de commits | Conventional commits en inglés imperativo: `feat:`, `fix:`, `docs:`, `test:`, `chore:`, `refactor:` |
| Deploy | Vercel, con integración continua desde GitHub. Preview por rama, producción en el merge a `main` |
| Protección de deploys | Vercel Authentication activa **solo en previews** (`ssoProtection: preview`). Producción es pública, sin login — necesario para F15.1 de la spec |
| URL de producción | https://clack-liart.vercel.app |
| Terminal | Git Bash (MINGW64). Los comandos de git que escriben historial o tocan el remoto los ejecuta Hernán |
| Protección de `main` | CI (`test`) y el deploy de Vercel (`Vercel`) como required status checks, sin aprobación obligatoria (Hernán trabaja solo), sin push directo, `delete_branch_on_merge` activo |

## Comandos de npm

| Comando | Qué hace |
|---|---|
| `npm run dev` | Servidor de desarrollo de Vite |
| `npm run build` | `tsc -b` (proyecto `src/`) + `vite build` |
| `npm run preview` | Sirve el build de producción localmente |
| `npm run lint` / `lint:fix` | ESLint sobre todo el repo |
| `npm run format` / `format:check` | Prettier |
| `npm run typecheck` | `tsc -b` (`src/`) + `tsc -p api/tsconfig.json` (Functions, NodeNext) |
| `npm test` / `test:watch` | Vitest |

## Stack fijado

Las versiones están decididas y no se cambian sin un motivo explícito.

| Pieza | Versión / nota |
|---|---|
| React | 19 |
| TypeScript | **Pineado a `<6.1.0`** (hoy resuelve 6.0.3). TS 7 existe pero `typescript-eslint` todavía no lo soporta (`peerDependency: >=4.8.4 <6.1.0`); no volver a 7.x hasta que typescript-eslint lo permita. `strict: true`, más `noUncheckedIndexedAccess` y `exactOptionalPropertyTypes`. `baseUrl` está deprecado desde la 6.0 (se remueve en la 7): los `paths` de `tsconfig.app.json` ya son relativos (`./src/*`) sin `baseUrl`, así que el fix sigue valiendo en cualquiera de las dos versiones |
| ESLint | **Pineado a `<10.0.0`** (junto con `@eslint/js`), hoy resuelve 9.39.x. ESLint 10 existe pero `eslint-plugin-jsx-a11y` todavía solo declara soporte hasta `^9`; no subir hasta que jsx-a11y lo permita |
| Vite | Build tool |
| Tailwind CSS | v4, configuración CSS-first con `@import "tailwindcss"` y `@theme`. **No** hay `tailwind.config.js` |
| React Router | **v8** (subido desde la v7 decidida en la Planificación: v8 solo elimina el paquete `react-router-dom`, que ya no usábamos — el patrón de imports es idéntico). Se importa de `react-router`; `RouterProvider` viene de `react-router/dom`. **No** se usa `react-router-dom` |
| Estado global | Context API + useReducer. Dos contextos separados: autenticación y carrito |
| Backend de datos | Firebase Auth + Firestore |
| Serverless | Vercel Functions en `api/`, handlers tipados con `@vercel/node` |
| Imágenes | AWS S3 con presigned URLs. Las credenciales de AWS viven solo en la Vercel Function |
| Testing | Vitest + React Testing Library, más `@firebase/rules-unit-testing` para las security rules |
| Validación | Zod. Los tipos se derivan con `z.infer`, nunca se duplican |

## Decisiones que se consultan seguido

- **El rol del usuario** vive en `users/{uid}.role` en Firestore como fuente de verdad, y se
  espeja a un **custom claim** de Firebase Auth. Las security rules leen
  `request.auth.token.role`, que no cuesta lecturas; un `get()` dentro de una regla sí se factura.
- **El rol se asigna con un script local de operador**, no con un endpoint HTTP. Un endpoint que
  otorga privilegios es una vía de escalación de permisos.
- **La ausencia del claim es el default seguro**: las reglas comparan contra `'admin'`, así que un
  usuario sin claim nunca es administrador.
- **El carrito** admite invitados: vive en `localStorage` validado con Zod y se fusiona con el de
  Firestore al iniciar sesión.
- **Las órdenes guardan un snapshot** del producto (nombre, precio, imagen), no una referencia. Un
  cambio de precio no puede mutar una orden pasada.

El detalle y las alternativas descartadas están en `docs/adr/`.

## Dónde está qué

| Archivo | Contenido |
|---|---|
| `docs/spec.md` | Alcance, criterios de aceptación, superficies |
| `docs/arquitectura.md` | Entidades del dominio y vocabulario |
| `docs/adr/` | Una decisión cara de revertir por archivo. No se editan: si la decisión cambia, se escribe un ADR nuevo |
| `docs/pendientes.md` | Deuda técnica detectada durante el ciclo que no bloquea el merge de la slice, pero necesita acción antes del Cierre |
| `DESIGN.md` | Sistema visual global. Lo gestiona Impeccable |
| `.impeccable/surfaces/` | Un brief por superficie (pública, privada de usuario, administración) |
| `shared/schemas/` | Contrato Zod único, importado por `src/` y por `api/` |
| `firebase.json` / `.firebaserc` | Configuración del Emulator Suite (Firestore puerto 8080, Auth puerto 9099) y del proyecto real `clack-add2a` |
| `firestore.rules` | Arranca cerrada (`allow read, write: if false`). Cada slice abre solo lo que necesita |
| `scripts/seed.ts` | Carga el catálogo de desarrollo en el emulador. Requiere el emulador corriendo (`firebase emulators:start`) |
| `tests/rules/` | Tests de security rules con `@firebase/rules-unit-testing`, corren aparte con `npm run test:rules` |
| `scripts/` | `seed.ts` para el catálogo de desarrollo, `grant-admin.ts` para asignar el rol admin |
| `.vercelignore` | Excluye `api/**/*.test.ts` del deployment (ver nota en la slice 10, abajo) |

## Herramientas de sesión

- **Plugin `firestore-native`**: encendido mientras dure el proyecto.
- **MCP de Vercel**: logs de runtime y errores agrupados cuando falla un deploy.
- **MCP de chrome-devtools**: capturas de referencia en la etapa de diseño, y consola, red,
  performance y accesibilidad en el recorrido del Cierre.
- **Context7**: se consulta por iniciativa propia antes de afirmar algo sobre una librería del
  stack.

## Estado

**Etapa 2 (Andamiaje) completa.** Etapa 3 (Ciclo) en curso.

**Slice 1 · Catálogo (listado)**, rama `feature/catalog`: capa de datos (converter, `getActiveProducts`, `useActiveProducts`) más la superficie visual construida con Impeccable contra el brief de `.impeccable/surfaces/` — grilla, tarjeta, skeletons, estados vacío/error. Pasó finish review (`ship`) y quedó documentada en `DESIGN.md`. Fuera de alcance a propósito: buscador, riel de categorías y la pieza destacada del hero (van con el filtro real en la slice 2 y con la landing en la slice 14); las tarjetas todavía no son interactivas (sin detalle de producto ni carrito construidos aún).

**Slice 2 · Filtro por categoría + búsqueda por nombre**, rama `feature/catalog`: `getActiveProducts` acepta `{ category?, searchTerm? }` y arma la consulta de Firestore con dos caminos de orden (por `createdAt` sin búsqueda, por `nameLower` con búsqueda, coincidencia de prefijo únicamente), con sus tres índices compuestos nuevos en `firestore.indexes.json`. Estado en la URL (`?category=&q=`) vía `useSearchParams`; la búsqueda tiene debounce (400ms) antes de tocar la URL y Firestore. Verificado en vivo contra el emulador seedeado (Chrome DevTools MCP): filtro, búsqueda por prefijo, combinación de ambos, debounce, recarga con filtros en la URL, y las dos variantes del estado vacío. Pasó el ciclo de finish review de Impeccable (`fix` → dos rondas de correcciones → verdict) y quedó documentada en `DESIGN.md`.

Dos decisiones de la superficie visual, citadas acá porque el proyecto no lleva comentarios en el código (y ya reflejadas en `DESIGN.md`):
- **El buscador y el riel de categorías viven en `CatalogPage`, no en el `<header>` compartido de `PublicLayout`** (que el brief de superficie describe como parte del mismo bloque visual). Se decidió así porque `PublicLayout` también envuelve el detalle de producto y la futura landing, y ninguna de esas pantallas necesita el filtro de categorías; el buscador se renderiza pegado al header compartido para que visualmente se lea como un solo bloque, pero el filtrado es lógica propia del catálogo.
- **El punto de color del riel de categorías es un adorno de ritmo visual, no un identificador único por categoría**: cicla las cuatro piezas cerradas (lima/magenta/cian/ámbar) sobre seis categorías, así que dos pares repiten color (sin que se toquen entre sí en el riel). El nombre en texto es quien distingue la categoría sin ambigüedad; el color acompaña el escaneo rápido, igual que `displayColor` en las tarjetas de producto no mapea 1 a 1 con ningún atributo del producto.

De paso, arreglado un gap de configuración de testing que no era de esta slice pero salió a la luz al escribir el primer test que monta el mismo componente más de una vez en el archivo: `src/test/setup.ts` no limpiaba el DOM entre tests (`@testing-library/react` solo hace `cleanup()` automático si `vitest.config` tiene `test.globals: true`, que este proyecto no usa a propósito). Sin eso, un `render()` en un test dejaba su árbol montado y el siguiente test lo veía. Se agregó un `afterEach(cleanup)` explícito en el setup — sin tocar `globals`.

**Slice 3 · Detalle de producto**, rama `feature/catalog`: `Product` suma `specs: { label, value }[]` y `curatorialNote: string` (contrato ampliado, `scripts/seed.ts` reescrito con datos reales para los 18 productos). Capa de datos nueva: `getProductById` (lectura puntual con `getDoc`, `null` si no existe o está inactivo — F3.2) y `useProduct`, más un `useKeyedAsync` genérico extraído de `useActiveProducts`/`useProduct` (mismo patrón de "loading derivado" de la slice 2, ahora compartido). La cartela (imagen a sangre + panel de papel con specs punteadas, nota curatorial, precio y selector de cantidad) se construyó con Impeccable, pasó el ciclo de finish review (`fix` → correcciones, incluyendo un bug real que encontré y arreglé yo mismo — ver abajo — → `ship`) y quedó documentada en `DESIGN.md`, incluyendo una regla nueva ("Fixed Paper Rule": la cartela es bone/ink fijo, nunca cambia con el tema). Pasó `/simplify` (4 ángulos): extraje `useKeyedAsync`, `formatPrice` y `assertFromServer` compartidos, colapsé las 4 ramas de `<main>` de `ProductDetailPage` en una, y bajé el estado de `quantity` al propio `QuantitySelector` (ya no fuerza un re-render de toda la página por cada click). Verificado en vivo contra el emulador seedeado: navegación desde la grilla, specs, nota curatorial, reseñas (con y sin), estado "no encontrado", stock agotado, tope de stock en el selector, sin errores de consola en los dos temas.

Decisiones de alcance y de contenido, citadas acá porque el proyecto no lleva comentarios en el código (y ya reflejadas en `DESIGN.md`):
- **F3.3 (reseñas) y F3.4/F3.5 (carrito) dependen de features que no existen todavía** (reviews es la slice 9, carrito la slice 6). Se construyó lo que no depende de ellas: promedio + cantidad de reseñas reales con link a `/products/:id/reviews` (ruta placeholder, mismo patrón que login/account/admin), y el selector de cantidad + botón "Agregar al carrito" correctamente acotados por stock (deshabilitado y con "Sin stock" en 0, tope en el stock disponible), pero el botón todavía no hace nada al clickear — la lógica real de carrito llega en la slice 6.
- **"Línea de para-quién en itálica" + "párrafo de criterio"** (dos elementos separados en el brief de superficie) se unificaron en un solo campo (`curatorialNote`), un párrafo que ya incluye el razonamiento de "para quién sí/no" en la prosa. Simplificación de modelo de contenido, no un olvido.
- **"Número de pieza y fecha de ingreso"** de la cartela: no se construyó. No existe un identificador de pieza estable fuera del índice arbitrario de la grilla (que cambia con el filtro/orden), y ningún criterio de F3 lo pide.
- **La página de detalle tiene dos `<h1>`** (el wordmark de `PublicLayout` y el nombre del producto en la cartela): decisión deliberada, no error — una página de un solo ítem merece su propio heading real en vez de depender solo del wordmark del sitio. Candidato a resolver demoviendo el wordmark a un elemento no-heading la próxima vez que se toque `PublicLayout`; no se hizo ahora porque es un cambio que afecta a todas las páginas públicas, no solo a esta.
- **Bug real encontrado y arreglado durante el finish review**: el selector de cantidad usaba `border-ink dark:border-bone` (el patrón normal de esta app), pero vive dentro de la cartela, que es bone/ink fijo y nunca cambia con el tema — en dark mode el borde quedaba invisible (bone sobre bone). Se arregló con `border-current`, que hereda el color de texto ambiente en vez de asumir el swap de tema de la página.
- **Hallazgo de `/simplify` descartado a propósito**: una revisión sugirió partir el schema de `Product` en `ProductSummary`/`ProductDetail` para que el catálogo no traiga `specs`/`curatorialNote` en cada lectura de listado. No se hizo: Firestore cobra por documento leído, no por campo, así que el costo real hoy es transferencia de red marginal (textos cortos, ~20 productos), no lecturas facturables extra. Partir el tipo tocaría el converter, los dos servicios, el seed y los fixtures de test para un ahorro que no existe a esta escala. Reconsiderar si el catálogo crece mucho o si se agregan campos pesados (medios, descripciones largas) solo para el detalle.

**Slice 5 · Registro, login, logout**, rama `feature/auth-screens`: `User` nuevo en `shared/schemas/user.ts` (`uid`, `email`, `displayName`, `role`, `createdAt`), converter espejo de `product.ts`, y `firestore.rules` abre `users/{uid}` (`create` solo con `role: 'customer'` y el propio uid, `update` con `role` inmutable — F13.4, con tests de reglas nuevos). Capa de servicios: `registerWithEmail`, `loginWithEmail`, `signInWithGoogle`, `requestPasswordReset`, todas devolviendo un `AuthResult` discriminado en vez de tirar excepción (son fallos esperables, no excepcionales). Pantallas construidas con Impeccable sobre `AuthPageLayout` (columna centrada, sin cartela: estas pantallas viven directo sobre el fondo tinta/hueso de la página, no se extendió la Fixed Paper Rule acá) — `LoginForm`, `RegisterForm`, `ForgotPasswordForm` (toggle in-place, sin ruta propia) y `GoogleSignInButton`, todos con `react-hook-form` + `mode: 'onTouched'`. Primitivos nuevos en `components/ui/`: `TextField` (label + hint/error con `aria-describedby` compartido, nunca los dos a la vez) y `Button` (`border-current`, `loadingLabel` configurable). `ProtectedRoute`/`AdminRoute` ganaron "volver al destino original" tras loguearse (F13.1, state de router validado con Zod en `redirectState.ts`, redirect factorizado en `RedirectToLogin`) y `AdminRoute` reemplazó el redirect mudo a `/` por `AccessDeniedState` (F13.2, pantalla nueva con su propio header vía `SiteHeader`, extraído de `PublicLayout` para poder reusarlo). Pasó el ciclo completo de finish review de Impeccable (`recapture` por evidencia vieja del catálogo → capturas correctas → `fix`, 5 hallazgos → `ship`) y `/simplify` (4 ángulos, hallazgos aplicados y descartados detallados abajo). Quedó documentada en `DESIGN.md`. Verificado en vivo contra el emulador seedeado en los dos temas y en mobile: registro, login, Google (vía el widget fake del emulador), logout desde página pública y desde una ruta protegida, recuperación de contraseña (con y sin cuenta existente), el rebote a `/admin` con un customer, y la vuelta al destino original tras loguearse — sin errores de consola.

Dos bugs de carrera reales, encontrados y arreglados durante la verificación en vivo (no los detectó ningún test, los agarró recorrer la app):
- **Login/registro rebotaba de vuelta a `/login`**: el `onSubmit` navegaba apenas el service call resolvía `ok:true`, sin esperar a que el listener de `AuthContext` confirmara `authenticated` — `ProtectedRoute` todavía veía `anonymous` en ese instante y volvía a redirigir. Arreglado desacoplando la navegación del resultado del submit: `useRedirectWhenAuthenticated(to)` (ahora vive dentro de `AuthPageLayout`, no en cada página) observa `auth.status` y navega recién cuando el contexto confirma la sesión.
- **Cerrar sesión desde una ruta protegida terminaba en `/login`, no en el catálogo** (violaba F1.7): `useLogout` esperaba a que `signOut()` resolviera y recién ahí navegaba a `/`, pero mientras tanto `ProtectedRoute` ya había reaccionado al cambio a `anonymous` y redirigido a `/login` primero. Arreglado invirtiendo el orden: `useLogout` navega a `/` (ya autenticado, ninguna guardia lo bloquea) y recién después llama a `auth.logout()`.

Decisiones de alcance y de contenido, citadas acá porque el proyecto no lleva comentarios en el código (y ya reflejadas en `DESIGN.md`):
- **react-hook-form + `@hookform/resolvers/zod`** para este y los próximos formularios (checkout, productos, reviews), decidido explícitamente en el contrato de esta slice en vez de controlados a mano — el proyecto ya lo dejaba evaluable, y con el perfil de performance de Hernán pesó más evitar el re-render por tecla.
- **"Email enumeration protection" no se activó del lado del proyecto de Firebase** (verificado con Context7 que es una config de servidor, no un toggle de consola): en cambio, el mismo resultado se garantiza en el cliente — `loginWithEmail` siempre muestra el mismo mensaje genérico exista o no la cuenta, y `requestPasswordReset` siempre confirma el envío, tragando a propósito el `auth/user-not-found` que el emulador (y probablemente producción) sigue devolviendo. Más robusto que depender de una config externa: funciona igual en el emulador y en producción sin tener que verificarlo dos veces.
- **El registro por email pide "Nombre"** aunque F1.1 no lo pide textualmente, para que el documento de usuario tenga la misma forma que el que produce Google (que sí entrega `displayName`) — F1.4 exige que ambos caminos produzcan el mismo documento.
- **`AccessDeniedState` obtuvo su propio header vía un `SiteHeader` nuevo**, extraído de `PublicLayout`, porque `AdminRoute` la renderiza sin ningún layout ancestro (es una guardia de ruta, no una ruta hija de `PublicLayout`) — sin esto quedaba la única pantalla del sitio sin nav ni wordmark. `PrivateLayout` sigue con su propio header a mano (no con `SiteHeader`): su wordmark es un `<Link>` de vuelta al catálogo (la única salida del área privada), mientras que el de `SiteHeader` es un `<h1>` no clickeable — diferencia real, no descuido. Sí comparten el control de logout (`LogoutButton`, extraído durante `/simplify`).
- **Hallazgos de `/simplify` aplicados**: `LogoutButton` compartido (antes duplicado en `AuthNav`/`PrivateLayout`); `InlineError` compartido (4 copias idénticas del mismo `<p role="alert">`); `loginWithEmail` dejó de retipear strings que ya vivían en `authErrorMessages.ts`; `signInWithGoogle` ahora pasa la lectura de existencia por `assertFromServer` antes de decidir si crea el documento; `registerWithEmail` paraleliza `updateProfile`/`createUserDocument` con `Promise.all` (no dependen entre sí); `ForgotPasswordForm` dejó de repetir el botón "volver a iniciar sesión" en sus dos ramas; el `watch()` del largo de contraseña en `RegisterForm` se aisló en un subcomponente (`RegisterSubmitButton` + `useWatch`) para no re-renderizar el formulario entero por tecla — casualmente esto también hizo desaparecer el warning de React Compiler que `watch()` disparaba; `useRedirectWhenAuthenticated` se centralizó en `AuthPageLayout` en vez de repetirse en cada página; `ProtectedRoute`/`AdminRoute` comparten `RedirectToLogin` en vez de reimplementar el redirect-con-retorno cada uno.
- **Hallazgo descartado a propósito**: una revisión sugirió sacar el `watch()` del password en `RegisterForm` directamente, apoyándose solo en la validación de Zod. No se hizo: F1.3 exige que el botón quede deshabilitado **antes** de intentar enviar, no recién después de un submit fallido — Zod solo dispara al enviar. Se resolvió el costo de performance real (aislar el re-render) sin sacrificar el criterio de aceptación.
- **Renombrar `AuthResult.field` a un tipo `RegisterResult` aparte**: quedó pendiente, no aplicado. Es un matiz de nombres sin impacto funcional (ningún otro consumidor lee ni necesita `field`) y los dos revisores de `/simplify` no coincidieron en si valía la pena — se prioriza el resto de los hallazgos, que sí tenían costo real.

**Slice 4 · Paginación del catálogo** *(extra)*, rama `feature/product-pagination`: `getActiveProducts` pide `PRODUCTS_PAGE_SIZE + 1` documentos (bajado de 24 a 8, para que el catálogo sembrado de 18 productos pagine de verdad) y devuelve `{ products, lastDoc, hasNextPage }` — el documento de más sirve solo para saber si hay página siguiente sin una query de agregación aparte (que Firestore factura extra), y se descarta antes de devolver la página. Acepta un cursor opcional (`QueryDocumentSnapshot`, verificado con Context7 que es la forma recomendada por sobre pasar el valor crudo del campo, que puede saltear resultados si dos documentos empatan). `useActiveProducts(filters, page)` reutiliza `useKeyedAsync` para el estado loading/error/success/retry (mismo patrón que el resto de los hooks del catálogo) y le pasa como `fetcher` una función que resuelve la página pedida contra un cache de páginas ya vistas (`Map<numeroDePagina, ProductsPage>`, en un ref, reiniciado cuando cambia el filtro): volver a una página ya visitada no vuelve a leer Firestore, y un link directo a `?page=N` (recarga, deep link) encadena los fetches necesarios — siempre con `startAfter()`, nunca con un salto de índice (F4.2). `page` vive en la URL (`?page=`), se omite en la página 1, y se resetea al cambiar de categoría o de búsqueda (F4.5). Nuevo `PaginationControls` (Anterior/Siguiente) reutiliza el `Button` de la slice 5 sin agregar nada al sistema visual. Pasó `/simplify` (4 ángulos, ver abajo) y quedó documentada en `DESIGN.md`. Verificado en vivo contra el emulador seedeado: avanzar/retroceder página, recarga en `?page=3` (F4.3), último botón deshabilitado en la última página (F4.4), los dos caminos de consulta paginan (con y sin búsqueda, F4.6), reset de página al cambiar filtro/búsqueda, sin errores de consola en desktop, mobile y los dos temas.

Decisiones y hallazgos de `/simplify`, citados acá porque el proyecto no lleva comentarios en el código:
- **Tamaño de página bajado a 8** (de los 24 originales de la slice 2): con 24, los 18 productos sembrados nunca llegaban a paginar y F4 quedaba sin poder probarse de verdad. 8 da ~3 páginas contra el catálogo actual, suficiente para ejercitar avanzar/deshabilitar en la última.
- **Reconciliar "página en la URL" con paginación por cursor** (tensión real entre F4.2 y F4.3): un link directo a una página posterior camina secuencialmente desde la última página cacheada hasta la pedida, encadenando `startAfter()` en cada salto — nunca un salto de índice — a costa de N lecturas chicas en ese caso puntual. Dado el tamaño real del catálogo (~20-40 productos), el costo es trivial.
- **Bug real encontrado por el agente de altitude durante `/simplify` y arreglado de paso**: pedir una página más allá de la última real hacía que el cursor se reseteara a `undefined` a mitad de la caminata, reiniciando la consulta desde la página 1 pero mostrándola con la etiqueta de la página pedida (ej. "Página 999" mostrando los productos de la página 1, con "Siguiente" habilitado incorrectamente). Se arregló deteniendo la caminata en cuanto una página devuelve `hasNextPage: false`, así una página fuera de rango se clampea a la última página real en vez de reiniciar el recorrido. Test de regresión agregado.
- **`useActiveProducts` pasó a reusar `useKeyedAsync`** en vez de reimplementar a mano el mismo patrón de loading derivado (tag de resultado + comparación de key en el render) que ya existía — encontrado por el agente de reuse, quien notó que las dos copias ya habían empezado a divergir en sus arrays de dependencias.
- **Nuevo `react-hooks/refs` (eslint-plugin-react-hooks 7) prohíbe tocar un ref durante el render**, incluso el patrón "resetear ref cuando cambia una key derivada de props" que las guías viejas de React consideraban seguro. El reset del cache de páginas se movió adentro de la función `fetcher` que le paso a `useKeyedAsync` (que solo corre dentro de su propio efecto), no en el cuerpo del hook.
- **Costo real evitado**: escribir en el buscador estando en la página 2+ antes desperdiciaba lecturas — pedía la página vieja con el filtro nuevo (cache invalidado, camina de la página 1 a la 2 innecesariamente) antes de que la URL se corrigiera a la página 1 un render después. Ahora `CatalogPage` pide la página 1 directamente en cuanto el término de búsqueda tiene un debounce pendiente, sin esperar el round-trip de la URL.
- **Hallazgo descartado a propósito**: una revisión sugirió sacar por completo la caminata secuencial del deep-link y reemplazarla por guardar el cursor codificado en la URL. No se hizo: un `QueryDocumentSnapshot` es un objeto vivo del SDK que no se puede serializar a la URL sin inventar una codificación propia, y el costo real de la caminata (unas pocas lecturas chicas, solo en el caso de un link directo a una página profunda) no lo justifica a esta escala.
- **Sin ronda de finish review dedicada**: `PaginationControls` reutiliza el `Button` ya auditado en la slice 5 sin agregar nada al sistema visual; el detector mecánico de Impeccable no encontró nada y la verificación visual en vivo (dos temas, mobile) confirmó que encaja con las convenciones ya establecidas. Se documentó en `DESIGN.md` igual, para que quede registrado como componente nuevo.

**Hallazgo de deuda técnica al revisar el preview de la PR #18** (no bloqueante, no es una regresión de esta slice — el mismo comportamiento ya existía en producción antes del merge): el catálogo público falla tanto en el preview como en producción porque `firestore.rules` nunca se desplegó al proyecto real (`clack-add2a`), que sigue cerrado por default, y `scripts/seed.ts` solo corrió contra el emulador. Registrado en `docs/pendientes.md` con la acción concreta a tomar antes del Cierre.

**Slice 6 · Carrito de compras**, rama `feature/cart-checkout`: `shared/schemas/cart.ts` define `CartItem` (`{productId, quantity}`), `GuestCart` (`{items}`, para `localStorage`) y `Cart` (`GuestCart` + `updatedAt`, para Firestore) — sin precio: el carrito siempre resuelve nombre/precio/stock contra el catálogo en vivo (ADR implícito, documentado en `docs/arquitectura.md` desde la Planificación). Estado global con **Context API + useReducer** (`CartContext`/`cartReducer`, ADR 0002): el reducer es puro (`ADD_ITEM` acumula cantidad topeando al stock; `SET_QUANTITY` topea a `[1, stock]` — no existe un estado de cantidad cero, "quitar" es una acción explícita separada; `REMOVE_ITEM`; `REPLACE`, usado para cargar/fusionar) y la persistencia vive enteramente en el provider. Carrito de invitado en `localStorage` (`guestCartStorage.ts`, `safeParse` de Zod con fallback a carrito vacío ante JSON corrupto o esquema viejo — F5.5) y fusión al iniciar sesión siguiendo ADR 0004 (`mergeGuestCart.ts`, cantidad **mayor** por producto, nunca la suma; excluye productos inactivos o sin stock informando cuál y por qué — F5.7; `loadAuthenticatedCart.ts` orquesta: lee el carrito de Firestore, fusiona si había invitado, escribe el resultado, **recién después** borra `localStorage`). `firestore.rules` abre `carts/{uid}` (lectura/escritura solo del propio dueño, sin distinción create/update porque no hay campos inmutables como en `users`), con tests de reglas nuevos. Capa de datos: `getCart`/`setCart` (documento único por uid) y `getProductsByIds` (una sola consulta `where(documentId(), 'in', ids)`, en vez de N lecturas sueltas, para resolver nombre/precio/stock de todas las líneas del carrito a la vez).

Superficie visual construida contra el contrato de dirección ya escrito en la Planificación (`.impeccable/surfaces/src-routes-account-accountlayout-tsx.md`, la superficie "recibo continuo"), acotada a la sección de carrito únicamente — el stepper de checkout de tres pasos que describe el mismo brief es de las slices 7a/7b, todavía no existen. `CartPage` vive en `/cart`, dentro de `PublicLayout` (accesible sin sesión — F5.1), con `CartLineItem` (franja de color por línea, cantidad editable reutilizando `QuantitySelector` — que ganó un `initialQuantity` opcional para arrancar en la cantidad guardada en vez de siempre en 1 —, precio tabular, "Quitar"), separador punteado entre líneas (mismo lenguaje que la tabla de specs de la cartela) y total. Reutiliza `EmptyState`/`ErrorState`/`Button`/`formatPrice`/`FIELD_COLOR_CLASSES` sin tocar el sistema visual. `MergeExclusionsNotice` muestra y permite descartar los productos que quedaron afuera al fusionar (F5.7). `ProductCard` se reestructuró: pasó de ser un único `<Link>` a un `<div>` con el `<Link>` (imagen/nombre/descripción) más una fila hermana con precio y el botón "Agregar" (F5, un paso desde la grilla) — un `<button>` no puede anidarse dentro de un `<a>`. En el detalle de producto, el botón "Agregar al carrito" (ya estilado desde la slice 3, hasta ahora sin acción) se conectó vía `AddToCartControl`, que junta el `QuantitySelector` y el botón para que la cantidad seleccionada llegue al carrito (dos pasos desde el detalle, F5). `SiteHeader` suma `CartIndicator` (suma de cantidades, no de líneas; enlaza a `/cart`; F5.9).

Pasó `/simplify` (4 ángulos, hallazgos aplicados y descartados detallados abajo) y quedó documentada en `DESIGN.md`, incluyendo el finish review de Impeccable (`fix` → un hallazgo real → `ship`). Verificado en vivo contra el emulador seedeado: agregar desde la grilla y desde el detalle (respetando la cantidad elegida), cambiar cantidad, quitar, carrito vacío con link al catálogo, indicador del header actualizándose, fusión al registrarse con carrito de invitado no vacío, y **cerrar sesión vuelve al carrito de invitado vacío** (el de Firestore deja de leerse, mismo criterio que F1.7) — todo en los dos temas y en mobile/desktop, sin errores de consola.

Dos bugs de carrera reales, encontrados por los agentes de `/simplify` (no por ningún test) y arreglados antes de commitear:
- **Doble escritura a Firestore en cada login**: el efecto de fusión despachaba `REPLACE` y, en el render siguiente, el efecto de persistencia (que solo se frenaba mientras la fusión estaba "en vuelo") volvía a escribir el mismo carrito que `loadAuthenticatedCart` ya había guardado — porque el flag `mergeInFlightRef` se apagaba en el `.finally()` de la promesa, que corre *antes* de que React llegue a re-renderizar por el `dispatch`. Arreglado con un segundo flag (`skipNextPersistRef`) que se enciende justo antes de despachar `REPLACE` y se consume una sola vez en el efecto de persistencia, sin importar el orden de los microtasks.
- **Una fusión que resuelve tarde podía pisar un estado más nuevo**: si el usuario cerraba sesión (o cambiaba de cuenta) antes de que `loadAuthenticatedCart` resolviera, el resultado viejo igual se aplicaba al llegar. Arreglado con el mismo patrón "ignorar respuesta obsoleta" que ya usa `useKeyedAsync` (un flag local que el cleanup del efecto apaga, chequeado antes de despachar).

Hallazgos de `/simplify` aplicados, además de los dos de arriba: `AddToCartControl` pasó a usar el `Button` compartido en vez de reimplementar su estilo a mano; `CartPage` colapsó dos condicionales idénticos (`EmptyState` + link al catálogo) en un solo bloque; `getProductsByIds` dejó de usar un mensaje de error específico de carrito (copiado de `getCart`) siendo una función genérica de productos, reusada también desde la fusión; se sacó la acción `CLEAR` del reducer (sin ningún disparador en la app todavía — vuelve con la slice 7b, cuando el checkout necesite vaciar el carrito tras crear la orden).

Hallazgos descartados a propósito, con motivo: **debounce en la escritura a Firestore por cada cambio de cantidad** — sin una medición real que lo justifique (el volumen es de unos pocos clicks manuales, no de tecleo), y un debounce con cancelación en el cleanup arriesga perder el último cambio si se cierra la pestaña dentro de la ventana de espera. **Unificar en un solo helper las tres fórmulas de tope de stock** (`ADD_ITEM`, `SET_QUANTITY`, `mergeGuestCart`) — no son la misma regla: `SET_QUANTITY` topea también por abajo en 1 a propósito (no existe cantidad cero), mientras que agregar o fusionar nunca debe inventar una unidad de la nada si el stock es 0; la fórmula que sí es idéntica en los tres lugares es un `Math.min` de una sola línea, demasiado trivial para justificar una función aparte. **El estado local de `quantity` en `AddToCartControl`** "duplica" el que ya vive dentro de `QuantitySelector" — es el puente necesario para que el click en "Agregar" conozca la cantidad elegida (el mismo `onQuantityChange` que `QuantitySelector` ya exponía sin usar desde la slice 3); el re-render que provoca queda acotado a ese componente chico, sin reabrir el problema de re-render de página completa que la slice 3 resolvió. **Extraer una constante compartida para los botones-link secundarios** (el "Reintentar" de `ErrorState`, "Ver catalogo", "Entendido") — cosmético, de bajo valor, se pospone. **Deduplicar la consulta a `getProductsByIds`** que corre una vez durante la fusión y otra vez al resolver la vista del carrito — es un costo único por sesión de login (no recurrente, no por render), y evitarlo exigiría acoplar el cache de `useResolvedCart` con el flujo de fusión de `CartContext` por un ahorro marginal.

**Slice 7a · Stepper de checkout**, rama `feature/cart-checkout`: `shared/schemas/checkout.ts` define `ShippingDetails` (nombre, dirección, ciudad, código postal, teléfono), `PaymentDraft` (`cardholderName` + `method: 'card'` + `outcome: 'success'|'error'` — **nunca** número de tarjeta, vencimiento ni CVV, ni siquiera para descartarlo: el formulario simplemente no los pide) y `CheckoutDraft` (el borrador persistido, con `.refine()` doble que exige el invariante que el reducer mantiene — `shipping` no puede ser `null` fuera del paso de envío, `payment` no puede ser `null` en revisión — así un `localStorage` corrupto o editado a mano por devtools vuelve al estado inicial en vez de mostrar una revisión a medio completar). Estado **local a la página, no un tercer Context global**: `checkoutDraftReducer` (cuatro acciones: `SUBMIT_SHIPPING`, `SUBMIT_PAYMENT`, `EDIT_SHIPPING`, `EDIT_PAYMENT`) más `useCheckoutDraft(uid)`, que combina el reducer con persistencia en `localStorage` bajo la clave `clack:checkout-draft:{uid}` — a diferencia del carrito y la sesión (que sí son Context+reducer porque los consume toda la app), el borrador de checkout solo lo usa esta página, así que un Context nuevo habría sido alcance que el enunciado no pide; documentado acá en vez de con un ADR nuevo, por no ser una decisión cara de revertir.

Navegación de los tres pasos resuelta con un único selector puro, `getStepVisibility(draft, step)` (`'form' | 'summary' | 'hidden'`), consumido tanto por `CheckoutPage` (qué renderiza cada sección) como por `CheckoutStepIndicator` (qué paso es clickeable) — evita la duplicación de "¿este paso ya se alcanzó?" que existía en un borrador anterior de esta misma slice (ver `/simplify` abajo). El indicador **solo permite volver a un paso ya completado, nunca saltar adelante**: un paso completado se ve como `<button>` real; el paso activo y los todavía no alcanzados son texto plano sin control interactivo (nunca un botón deshabilitado sin explicación, F6.4); "Revisión" nunca es clickeable. Volver a editar un paso anterior no pierde los datos de los posteriores (F6.2), y al reenviar ese paso sin cambios el borrador salta directo de vuelta a revisión sin pedir repetir lo que ya estaba completo.

Superficie construida contra el mismo contrato de dirección de la slice 6 (`.impeccable/surfaces/src-routes-account-accountlayout-tsx.md`), resolviendo ahora la parte que esa slice dejó explícitamente sin construir: los tres pasos del "recibo continuo". `CheckoutPage` vive en `/checkout`, dentro de `PrivateLayout`/`ProtectedRoute` (a diferencia del carrito, el checkout exige sesión porque la orden futura necesita `userId`); el resumen del carrito de arriba se extrajo a `CartLinesSummary` (ver `/simplify`), compartido con `CartPage`. Debajo, un separador punteado (`divide-dotted`, mismo lenguaje que la tabla de specs y la lista del carrito) entre cada sección ya completada, siguiendo la tesis del brief de "un documento que crece", no pantallas por paso. `ShippingForm`/`PaymentForm` usan `react-hook-form` + `zodResolver` + `mode: 'onTouched'` (mismo patrón de la slice 5); `ShippingSummary`/`PaymentSummary` comparten la tarjeta+botón "Editar" vía `CheckoutSummaryCard`. El paso de pago pide solo nombre del titular y un radio "Resultado simulado: Aprobado/Rechazado" con texto explícito de que no hay cargo real — nada de número de tarjeta falso, que no aporta nada al criterio de aceptación (poder forzar éxito/error) y sí agrega ruido de seguridad a algo que jamás debe persistirse. El botón "Confirmar compra" de `ReviewSection` está construido y estilado pero **sin acción al hacer click** — mismo patrón que "Agregar al carrito" en la slice 3: la transacción real que crea la orden llega en la slice 7b.

**Resuelta la "decisión sin resolver" que el brief había dejado abierta para esta slice**: en 320px, `CheckoutStepIndicator` es `sticky top-0` (fondo opaco `bg-bone`/`dark:bg-ink` + hairline inferior, para que el contenido pase por debajo sin transparencias ni blur — nada de eso existe en el sistema) y el total de `CartLinesSummary` es `fixed inset-x-0 bottom-0` con el mismo tratamiento; los dos vuelven a estático/en flujo desde `md:`. Verificado con prueba numérica, no solo visual: achicando el viewport para generar suficiente scroll, `getBoundingClientRect().top` del indicador se clampea en `0` en dos profundidades de scroll distintas mientras el contenido de atrás cambia (capturas `checkout-sticky-proof-scroll550-shortviewport.png`/`...-650-...` en `.impeccable/review/`) — con el viewport real de 320×800 el documento no tiene scroll suficiente para probarlo de otra forma. El total fijo quedaba tapado por el banner propio del emulador de Firebase (fijo al pie con z-index más alto) durante las pruebas locales; confirmado con `getBoundingClientRect()` que es un solapamiento exclusivo del entorno de desarrollo, no del build de producción (el banner no existe fuera del emulador).

Pasó el ciclo completo de finish review de Impeccable: primera ronda `fix` (misma sticky/fixed de arriba, más un separador punteado que faltaba entre secciones), segunda ronda `fix` por evidencia insuficiente (capturas viejas de la slice 6, sin el estado de revisión ni el hueco vacío que la primera corrección había dejado al sacar el total del flujo — se corrigió acotando el borde/padding sobrante a `md:` únicamente), tercera ronda `ship` con evidencia numérica. `/security-review` sin hallazgos (ver detalle: el borrador de pago nunca colecciona datos de tarjeta, la clave de `localStorage` está bien particionada por uid, no hay `dangerouslySetInnerHTML`, `/checkout` está bien gateada, todos los campos de texto tienen `.max()`). Verificado en vivo contra el emulador seedeado: los tres pasos completos hasta revisión, volver a editar sin perder el pago ya cargado, validación de campo puntual sin botones deshabilitados sin explicación, recarga a mitad de checkout (F6.3), redirect a `/login` y vuelta a `/checkout` para un usuario sin sesión (reutiliza el mecanismo de la slice 5), carrito vacío sin renderizar ninguna sección, y **el borrador de checkout no se filtra entre dos cuentas distintas en el mismo navegador** (bug real encontrado en vivo, ver abajo) — todo en los dos temas, mobile y desktop, sin errores de consola.

**Bug real encontrado durante la verificación en vivo** (no por ningún test): el borrador de checkout vivía en una sola clave de `localStorage` sin distinguir usuario, así que loguearse con una segunda cuenta en el mismo navegador mostraba el envío y el pago que había tipeado la primera. Arreglado antes de escribir los tests, parametrizando la clave por uid (`clack:checkout-draft:{uid}`) — `CheckoutPage` solo se monta autenticado, así que siempre hay un uid real disponible.

**Bug preexistente de la slice 6, ajeno a esta pero encontrado al reusar `CartLineItem` en `/checkout`, arreglado con aprobación explícita**: a 320px la fila de cantidad/precio/"Quitar" se cortaba fuera del viewport (`flex ... justify-between` sin wrap, tres elementos que no entran en el ancho disponible). Arreglado permitiendo que esa fila haga wrap en mobile (`flex-wrap`, vuelve a una sola línea desde `md:`) — fix mínimo y contenido, sin rediseñar la fila; una reestructuración más profunda (grid con tracks explícitos) queda para si algún día se agrega un cuarto elemento a esa fila.

Hallazgos de `/simplify` aplicados: `useCheckoutDraft` escribía a `localStorage` incluso en el primer render, reescribiendo lo que acababa de leer — se agregó un guard de "saltar la primera escritura" (mismo patrón que `skipNextPersistRef` de `CartContext`). La lógica de "¿este paso se ve como formulario/resumen/oculto?" estaba duplicada con fórmulas ligeramente distintas en `CheckoutPage` y `CheckoutStepIndicator` (encontrado independientemente por dos agentes) — unificada en el selector `getStepVisibility` de arriba. `CheckoutPage` duplicaba casi entero el bloque de carrito de `CartPage` (skeleton/error/vacío/lista+total) — extraído a `CartLinesSummary` (`src/features/cart/components/`), con una prop `totalVariant` (`'static'` para el carrito, `'sticky-bottom'` para el checkout). El link "Ir a pagar" nuevo de `CartPage` había retipeado a mano la clase completa de `Button` — se exportó como `BUTTON_CLASSES` (mismo patrón que `AUTH_NAV_LINK_CLASSES`). `ShippingSummary`/`PaymentSummary` duplicaban la tarjeta+botón "Editar" — extraído a `CheckoutSummaryCard`.

Hallazgos de `/simplify` descartados, con motivo: **extraer un helper genérico de lectura/escritura de `localStorage` compartido con `guestCartStorage.ts`** — implicaría tocar un archivo de la slice 6 ya mergeada para una abstracción que hoy tendría un solo consumidor real; dos copias de un idioma de ~15 líneas no justifican eso todavía, se reconsidera si una tercera feature lo necesita. **Que `useCheckoutDraft` reaccione a cambios de `uid` en caliente** (mirando el patrón de fusión de `CartContext`) — Firebase Auth siempre pasa por un estado anónimo intermedio al cambiar de cuenta (no se puede estar logueado como dos usuarios a la vez sin un logout explícito primero), y `ProtectedRoute` ya desmonta el árbol completo en ese instante; el mecanismo que se sugería nunca dispararía distinto de lo que un montaje nuevo ya hace hoy.

**Slice 7b · Transacción de compra**, rama `feature/checkout-order`: cierra F6 conectando el botón "Confirmar compra", inerte desde la slice 7a. **Decisión de arquitectura central, documentada en el [ADR 0007](docs/adr/0007-creacion-de-la-orden-via-vercel-function-con-admin-sdk.md)**: la orden se crea desde una Vercel Function nueva (`api/orders/create.ts`) con el Admin SDK dentro de una única `runTransaction`, no desde una transacción del cliente — mismo motivo que el ADR 0006: validar entre varios documentos a la vez (¿el stock que se descuenta coincide con la orden que se crea?) es frágil en security rules y confiar en un total que manda el cliente abre la puerta a fabricar cualquier orden. `firestore.rules` abre `orders/{orderId}` solo a lectura del propio dueño (F7.1/F7.2, preparado para la slice 8); ninguna escritura de cliente está permitida, ni ahí ni en `products/*` para stock — la única vía es el Admin SDK, que se saltea las reglas.

**Contrato de la función**: verifica el ID token (`Authorization: Bearer`), lee `carts/{uid}` real dentro de la transacción (nunca la lista que mande el cliente) para saber qué y cuánto comprar, y por cada línea lee el producto fresco y valida activo/stock/precio/cantidad contra `expectedItems` (lo que el cliente vio en la revisión, mandado solo para detectar diferencias, nunca para decidir montos). Cualquier violación aborta sin escribir nada y nombra el producto (F6.7 sin stock, F6.8 precio cambiado, más un código `CART_CHANGED` nuevo para cantidad desincronizada o una línea que el cliente nunca vio — ej. se agregó algo en otra pestaña después de cargar la revisión). Si todo es válido, en la misma transacción: crea `orders/{orderRequestId}` con snapshot de nombre/precio/imagen por línea, descuenta `stock`, incrementa `orderCount`/`unitsSold`, y vacía `carts/{uid}` (F6.5, F6.6). **Idempotencia con id pre-generado** (patrón que `checklist-db` pide explícitamente para checkout/pagos): `orderRequestId` es un UUID que el cliente genera una vez por borrador de checkout — vive en `CheckoutDraftState`, persiste en `localStorage` como el resto del draft — y es también el id del documento de la orden; un reintento con el mismo id encuentra que la orden ya existe y devuelve el mismo resultado sin descontar stock dos veces. Errores estructurados `{code, message, details?}` con ocho códigos (`UNAUTHENTICATED` 401, `INVALID_REQUEST`/`EMPTY_CART` 400, `PRODUCT_UNAVAILABLE`/`OUT_OF_STOCK`/`PRICE_CHANGED`/`CART_CHANGED` 409, `INTERNAL_ERROR` 500), logueados como JSON estructurado sin nunca volcar el body de la request (que lleva datos de envío).

**`/security-review` encontró dos hallazgos reales antes del merge**, ambos arreglados en el momento. **(1) HIGH, confirmado empíricamente contra el emulador**: la transacción leía `cart.items` sin agrupar por `productId` y validaba/descontaba stock por línea, no por producto — como `carts/{uid}` es escribible directo por el dueño sin validar unicidad, cualquier cliente autenticado podía escribir su propio carrito a mano (devtools) con muchas líneas del mismo producto y comprar más unidades que el stock real, porque todas las líneas se validaban contra el mismo stock leído una sola vez al principio de la transacción, y `FieldValue.increment()` llamado varias veces sobre el mismo documento dentro de una transacción **sí se acumula de verdad** (verificado con un script de reproducción contra el emulador: 50 llamadas de `increment(-1)` sobre un documento con `stock: 1` terminaron en `stock: -49`, sin que la transacción fallara). Arreglado agregando `cart.items` por `productId` (sumando cantidades) antes de validar y de escribir — un solo chequeo de stock y un solo `tx.update()` por producto, sin importar cuántas líneas duplicadas traiga el carrito. **(2) MEDIUM**: `verifyIdToken` no pasaba `checkRevoked: true`, así que un token robado seguía funcionando durante su vida útil (~1h) aunque la víctima cambiara su contraseña para intentar cerrar sesiones — exactamente el camino de auto-remediación que la slice 5 ya ofrece. Arreglado pasando `true` como segundo argumento (con el costo aceptado de una llamada extra al backend de Auth por compra, razonable para el endpoint de mayor riesgo del proyecto). Los dos hallazgos suman dos tests nuevos de regresión en `api/orders/create.test.ts`.

**Pago rechazado nunca llega al servidor (F6.9)**: si `payment.outcome === 'error'`, `ReviewSection` muestra el rechazo localmente y no llama a la función — cero riesgo, cero request. El servidor igual lo rechazaría si llegara (`createOrderRequestSchema` tiene un `.refine()` que exige `outcome === 'success'`), pero no hace falta gastar la invocación.

**`shared/schemas/order.ts`** nuevo: `Order`/`OrderItem`/`OrderStatus`, el contrato de request/response de la función, `SHIPPING_COST` (envío fijo, $4999, ~4-6% del producto más barato del seed) y `roundToCents` — movido ahí desde `useResolvedCart.ts` para que el cliente y la función redondeen con la misma función, no dos copias que puedan divergir.

**Bug real encontrado durante la verificación en vivo** (no por ningún test): después de confirmar una compra, si el usuario agregaba un producto nuevo al carrito y volvía a `/checkout`, la pantalla seguía mostrando la confirmación vieja en vez del formulario — el `useState` local de "pedido confirmado" nunca se invalidaba. Arreglado haciendo que la confirmación solo se muestre cuando **además** el carrito no tiene líneas reales (`confirmedOrderId && !(cart.status === 'success' && cart.lines.length > 0)`): en cuanto hay un ítem nuevo, la condición deja de cumplirse sola, sin necesidad de un efecto ni de un reset explícito.

**Decisión de scope explícita, para que no se lea como un olvido en la slice 8**: la confirmación se persiste en `sessionStorage` (`clack:last-order-id:{uid}`, muleta temporal para sobrevivir una recarga justo después de confirmar) en vez de vivir en el draft de checkout (`localStorage`). Un agente de `/simplify` (altitude) sugirió doblar esto directo en `CheckoutDraftState` como un paso terminal — se descartó a propósito: cambiaría el tiempo de vida de la confirmación (de "esta pestaña" a "indefinido hasta la próxima compra") y abriría una pregunta de diseño nueva y no resuelta (¿cuándo exactamente un borrador "confirmado" se considera superado por un checkout nuevo?) que no corresponde decidir dentro de un `/simplify`. La solución real y definitiva llega con la slice 8: `CheckoutPage` va a navegar a `/orders/:id` (una página real que lee la orden por id desde Firestore) en vez de mostrar un estado local, y `lastOrderStorage.ts` se **reemplaza**, no queda como código muerto al lado de la solución nueva.

**Reincorporada la acción `CLEAR` al `cartReducer`** (sacada en la slice 6 con la nota "vuelve en la 7b") — `handleOrderCreated` la dispara junto con `draft.resetDraft()` (acción `RESET` nueva en `checkoutDraftReducer`, con un `orderRequestId` fresco para el próximo checkout).

**Gotcha de entorno de desarrollo nuevo, sin precedente en ninguna slice anterior**: ninguna Function se había ejercitado antes desde el navegador (`api/health.ts` solo tenía test unitario). `vercel dev` no puede servir el frontend de este proyecto directamente — su proxy aplica el rewrite SPA de `vercel.json` a **todas** las requests, incluidas las internas de Vite (`?import`, `?t=...`), así que termina alimentando `index.html` al plugin `vite:import-analysis` como si fuera JS y tira un error de sintaxis en cada carga. Es un problema conocido y documentado de `vercel dev` con cualquier framework que use su propio dev server con query params internos (no es un bug de Vite 8 ni de este proyecto). Solución: `vite.config.ts` suma `server.proxy: { '/api': 'http://localhost:3000' }` (nueva, solo afecta al dev server, no al build de producción), y el flujo de desarrollo local para tocar `api/*` pasa a ser **dos procesos**: `vercel dev` sirviendo solo las Functions en el puerto 3000, y `npm run dev` (Vite normal) sirviendo el frontend, que proxea `/api/*` al primero. Además, el Admin SDK dentro de `vercel dev` necesita `FIRESTORE_EMULATOR_HOST`/`FIREBASE_AUTH_EMULATOR_HOST` en el mismo `.env` que lee `vercel dev` (no alcanza con un `.env.local` aparte, `vercel dev` no lo levantó en las pruebas) — las dos variables ya están documentadas en `.env.example`.

**Testing**: los tests de la función (`api/orders/create.test.ts`) corren con `firebase emulators:exec` (script nuevo `npm run test:functions`, mismo patrón que `test:rules`) contra el **emulador real de Firestore**, con solo `verifyRequestToken` mockeado — la transacción en sí, incluyendo lectura, validaciones y escrituras, es 100% real. Cubre el camino feliz, sin stock, precio cambiado, producto retirado, carrito vacío, carrito desincronizado, sin token, body inválido, pago rechazado rebotado por el servidor, reintento idempotente (mismo `orderRequestId` dos veces no descuenta dos veces), y reintento tras un fallo de negocio (un intento fallido no "gasta" el id). **Un test queda `it.skip` a propósito**: el de compra simultánea de la última unidad (regla 11) se reprodujo primero con `console.error` de diagnóstico y confirmó una sobreventa real (`stock: -1`, las dos llamadas con 200) — investigado con la documentación oficial de Firebase y varios issues abiertos de `firebase-tools` (#3928, #6758, #1628, #2452, #3969): es una limitación conocida y de larga data del emulador, que usa reintento optimista en vez del locking pesimista por documento de la Firestore real, así que dos transacciones concurrentes pueden leer el mismo stock viejo sin bloquearse. El código (`tx.get()` antes de `tx.update()`, dentro de una única transacción) es el patrón correcto según la documentación de producción; el test queda como documentación de la garantía real, saltado porque la herramienta no puede verificarlo, no porque el código esté mal.

Hallazgos de `/simplify` aplicados: dos `tx.get()` en paralelo (uno para orden+carrito, otro por cada producto del carrito) pasaron a `tx.getAll(...)` — un solo viaje de red en vez de varios, y una ventana de contención más chica en una transacción que ya sabíamos sensible a la concurrencia. El `status`/`errorMessage` de `ReviewSection` (dos `useState` que había que mantener sincronizados a mano) se colapsó en un solo tipo discriminado `SubmitState`. La construcción de "borrador fresco con un `orderRequestId` nuevo" estaba duplicada en `checkoutDraftStorage.ts` y en el reducer — unificada en `createFreshCheckoutDraftState()`, exportada desde `checkoutDraftReducer.ts` y reusada por los dos. Con `lastOrderStorage.ts` como tercer consumidor del idioma "clave por uid + try/catch silencioso" (los otros dos: `guestCartStorage.ts`, `checkoutDraftStorage.ts`), se cumplió el criterio que la propia slice 7a había dejado escrito ("se reconsidera si una tercera feature lo necesita") — extraído `src/lib/webStorage.ts` (`safeStorageGet`/`safeStorageSet`/`safeStorageRemove`, reciben el `Storage` como parámetro para servir tanto a `localStorage` como a `sessionStorage`), reusado por los tres.

Verificado en vivo con `vercel dev` + el emulador seedeado (no solo tests): compra completa de punta a punta con datos reales en Firestore (orden creada con snapshot correcto, `stock`/`orderCount`/`unitsSold` del producto actualizados, carrito vacío — confirmado leyendo los documentos directo del emulador vía REST), pago forzado a error (sin ningún request a `/api/orders/create`, carrito intacto), navegación "Ver mis órdenes" al placeholder, mobile 320px sin overflow, sin errores de consola en ningún paso — y el bug de la confirmación vieja de arriba, encontrado y arreglado en el momento.

**Slice 8 · Mis órdenes**, rama `feature/checkout-order`: cierra F7 (historial, detalle y cancelación de una orden propia) y **reemplaza el shim de la 7b** — `CheckoutPage` ahora navega a `/orders/:orderId` en vez de mostrar una confirmación local, y `OrderConfirmation.tsx`/`lastOrderStorage.ts` se borraron junto con sus tests, tal como quedó anotado en la 7b.

**Lectura (historial + detalle): Firestore directo desde el cliente, sin Function** — mismo criterio que `carts`/`products`, sin lógica de negocio de por medio. `getOrders(uid)` consulta `where('userId','==',uid).orderBy('createdAt','desc')` (primera query `list` de este proyecto contra una regla de propiedad, no solo `get`; nuevo índice compuesto en `firestore.indexes.json`, y dos tests de rules nuevos que ejercitan `list`, no solo `get`, sobre `orders`). `getOrderById(id)` mapea tanto "no existe" como "es de otro usuario" al mismo `null`/`not-found` — mismo criterio de no revelar existencia que `loginWithEmail` en la slice 5 — sin debilitar F7.2: Firestore sigue siendo quien deniega de verdad, la UI solo no distingue los dos casos en el mensaje. Sin paginación: el brief de superficie ya fija el rango realista ("0 a unas 20 órdenes"), reconsiderar si crece mucho.

**Cancelar: Vercel Function nueva (`api/orders/cancel.ts`) reusando el razonamiento de ADR 0007, sin ADR nuevo** — es la misma decisión ya registrada (invariante cruzado orden↔stock, el cliente no puede tocar ninguno de los dos directo) aplicada a un segundo endpoint. Verifica el token, lee la orden dentro de la transacción, exige `userId === uid` (si no, `ORDER_NOT_FOUND`, mismo código para "no existe" y "es de otro" — sin filtración de enumeración, aunque acá es defensa en profundidad más que un vector real: los ids son UUID) y `status === 'pending'` (si no, `INVALID_STATUS_TRANSITION` — nunca confía en que el botón esté oculto en el cliente). Si pasa: cambia el estado a `cancelled` y restituye el stock de cada línea. **`orderCount`/`unitsSold` nunca se revierten** — F7.7 dice literalmente "el stock vuelve a sumarse", y como esos dos campos quedan en ≥1 para siempre una vez que un producto tuvo una orden, ADR 0005 (guarda de hard-delete `orderCount === 0`) ya protege a ese producto de haber sido borrado antes del cancel.

**`/security-review` no encontró hallazgos.** Confirmó explícitamente que `cancel.ts` sigue el mismo patrón ya auditado en `create.ts`: ownership derivado solo del token verificado, estado revalidado dentro de la transacción, `ORDER_NOT_FOUND` uniforme sin fuga de enumeración, y el bug de líneas duplicadas de la 7b (agregar por `productId` antes de escribir) correctamente replicado acá con su propio test de regresión.

**Dos bugs reales encontrados durante la verificación en vivo, ninguno por los tests**:
- **El mismo gotcha de `vercel dev` + Vite de la 7b volvió a aparecer** (dos procesos separados, `vercel dev` solo para `/api` en el puerto que `vite.config.ts` ya proxea + `npm run dev` para el frontend) — esta vez porque los procesos de background de la sesión anterior no sobrevivieron entre llamadas de la herramienta de shell; se resolvió arrancando todo con la ejecución en background propia del harness en vez de backgrounding manual con `&`, y quedó documentado en la memoria del proyecto que el arranque debe usar ese mecanismo, no un subshell desatachado a mano.
- **`getOrderById.ts` nunca mapeaba el `permission-denied` a `null`, a pesar de tener el chequeo escrito.** El chequeo comparaba `err instanceof FirestoreError` (el tipo importado de `firebase/firestore`), pero el objeto que el SDK realmente lanza en runtime es un `FirebaseError` genérico (de `@firebase/util`), no una instancia de esa clase — confirmado en vivo instrumentando el `catch` temporalmente (`err.name` == `"FirebaseError"`, `err instanceof FirestoreError` == `false`, `err.code` == `"permission-denied"`). Sin el fix, leer la orden de otro usuario mostraba el `ErrorState` genérico ("No pudimos cargar la orden") en vez de "Orden no encontrada" — F7.2 seguía protegido por las reglas (nadie veía datos ajenos), pero la UX de no-enumeración que el contrato pedía estaba rota. Arreglado reemplazando el chequeo por `instanceof`/clase por un duck-type sobre `.code` (sin type assertions: el narrowing de `'code' in err` alcanza con TS ≥4.9). Test de `getOrderById.test.ts` actualizado para reflejar el mecanismo real en vez de mockear una clase que nunca se usa en producción.

**Modal nuevo en `components/ui/`** (`src/components/ui/Modal.tsx`), primer primitivo de overlay de este proyecto: overlay opaco + hairline (nunca sombra, mismo lenguaje que la Pinned Chrome Rule ya documentada en `DESIGN.md`), foco atrapado dentro del panel, Escape y click afuera cierran (deshabilitables durante un submit en curso), foco vuelve al disparador al cerrar — verificado en vivo con `evaluate_script` (Escape cierra y devuelve el foco al botón "Cancelar orden"). El foco inicial va al botón seguro ("Volver"), no al destructivo, para que un Enter reflejo no confirme una cancelación por accidente. Se construyó como primitivo reusable a propósito, no un one-off: el plan ya tiene tres confirmaciones destructivas más (borrar la reseña propia en la slice 9, hard-delete de producto en la 10, cancelación desde el admin en la 12).

**`OrderStatusBadge`** distingue los cuatro estados sin depender solo del color (F7.5): el texto siempre está (Pendiente/En proceso/Completada/Cancelada) y cada uno tiene además un tratamiento tipográfico propio (contorno / contorno+subrayado / relleno invertido / contorno+tachado) — ningún ícono nuevo, coherente con la convención de este sistema de diseño de resolver todo con texto y las reglas ya existentes (hairline, dotted, fill), sin abrir el set cerrado de los cuatro colores de campo a un uso semántico nuevo.

Hallazgos de `/simplify` aplicados (4 agentes en paralelo): `respondWithError` estaba duplicado entre `create.ts` y `cancel.ts` — movido a `api/_lib/orderErrors.ts`, que ya era el hogar de sus tres piezas. `cancelOrder.ts` reimplementaba entero el flujo fetch-con-token-y-parseo de `createOrder.ts` — extraído `postOrderRequest` a `src/lib/orderApiRequest.ts`, reusado por los dos. El tipo `SubmitState` (idle/submitting/error) estaba re-declarado igual en `CancelOrderModal` y `ReviewSection` — exportado desde `src/lib/asyncSubmitState.ts`. `OrderDetailPage` nombraba el resultado del hook `order`, forzando `order.order.X` en todo el archivo — renombrado a `detail` y extraído `OrderDetailContent`, mismo patrón que `ProductDetailContent`; de paso se sacó un chequeo de auth redundante en el botón de cancelar que no consumía `auth.user` (el que sí lo necesita, para pasarle `user` al modal, quedó como el único). Hallazgo de altitude real: `cancel.ts` restituía stock con un `tx.update()` ciego por línea, sin el `tx.getAll()` + chequeo de existencia que `create.ts` sí hace — un producto borrado después de la compra habría tirado la transacción entera con `INTERNAL_ERROR`, dejando la orden atascada en `pending` para siempre. Arreglado agregando por `productId` (mismo patrón que la 7b) y leyendo los productos antes de escribir; si alguno ya no existe, se saltea su restitución sin abortar el cancel. Dos tests de regresión nuevos (producto borrado, líneas duplicadas). El `Modal` también se endureció por el hallazgo de eficiencia: el listener de teclado dependía de `onClose`/`closeDisabled` por props, así que cualquier re-render del padre mientras el modal estaba abierto lo desmontaba y remontaba — estabilizado con un ref actualizado en un efecto aparte, el listener ahora se monta una sola vez.

Verificado en vivo con `vercel dev` + el emulador seedeado: compra completa → redirect a `/orders/:id` con los datos reales (confirmado en Firestore vía REST: `stock` descontado, `orderCount`/`unitsSold` sumados), listado en `/orders`, cancelar una orden pendiente (stock restituido, `orderCount`/`unitsSold` intactos, estado → `cancelled`, el botón de cancelar desaparece), un segundo usuario no puede leer la orden del primero (ahí se encontró el bug de `getOrderById` de arriba), historial vacío para una cuenta nueva, mobile 320px en el detalle y en el modal de cancelar, tema claro, sin errores de consola en ningún paso salvo el bug ya corregido.

**Slice 9 · Reseñas y calificaciones** *(extra)*, rama `feature/reviews`: cierra F8. Una reseña es un documento en `products/{productId}/reviews/{userId}` — el id del documento es el `uid` del autor, así que F8.2 ("volver a opinar reemplaza, nunca hay dos del mismo autor") y buena parte de F8.3 quedan garantizados por estructura, no por lógica de aplicación. La reseña se escribe **directo desde el cliente** (Firestore, sin Function) — a diferencia de las órdenes, acá no hay ningún invariante cruzado entre documentos que justifique el razonamiento de ADR 0007, así que no aplica y no se escribió ADR nuevo; es el mismo criterio que ya rige `carts/{uid}`.

**`ratingAverage`/`ratingCount` los sigue escribiendo solo el servidor (ADR 0006), vía una Vercel Function nueva** (`api/reviews/recalculate.ts`) que el cliente llama después de guardar/borrar. A diferencia de `create.ts`/`cancel.ts`, no corre dentro de una transacción: no hay ningún invariante que proteger contra una carrera, es un recálculo idempotente que ADR 0006 ya describe como "eventualmente consistente". Usa una consulta de agregación nativa del Admin SDK (`collection.aggregate({count: AggregateField.count(), average: AggregateField.average('rating')})`, confirmada con Context7) en vez de leer todas las reseñas a mano — sigue siendo "recalcular desde cero" tal como pide el ADR, solo que en una sola lectura agregada. `average` es `number | null` tipado por el propio SDK; se normaliza a `0` cuando no hay reseñas (test de regresión dedicado).

**Decisión de UI, la más importante de la slice: el "4.5 · 12 reseñas" que se muestra en la página de reseñas se calcula en el cliente a partir de la lista ya traída, no leyendo `product.ratingAverage`.** Dos agentes de `/simplify` (reuse y altitude) señalaron esto de forma independiente como una duplicación de lo que ADR 0006 ya resuelve — se evaluó explícitamente y se mantuvo: la página tiene la lista completa en memoria, así que calcular ahí es más preciso que leer el campo denormalizado (que solo se pone al día después de que la Function corre), y es la única forma de cumplir F8.4 ("se actualiza sin recargar la página") sin depender de ese timing. `product.ratingAverage`/`ratingCount` siguen siendo la fuente para el catálogo y la ficha de producto, que no tienen la lista cargada.

**Hallazgo real de `/security-review`, HIGH, encontrado y arreglado antes del merge: inyección de path en `api/reviews/recalculate.ts`.** `productId` llegaba validado solo como `z.string().min(1)`, sin cerrar el carácter `/`, y se interpolaba directo en un string de path del Admin SDK (`adminDb.doc(\`products/${productId}\`)`). Como el Admin SDK trata ese string completo como un path y lo parte por `/`, un `productId` como `"p1/reviews/uid-de-otro"` resolvía al documento de reseña real de otro usuario (mismo conteo par de segmentos que un producto) — y como las reseñas son públicas (`allow read: if true`) y exponen `userId`, cualquier cuenta autenticada podía enterarse del uid de una víctima y forzar una escritura (`ratingAverage`, `ratingCount`, `updatedAt`) sobre su documento de reseña, algo que `firestore.rules` rechaza de un cliente normal pero que el Admin SDK ejecuta sin pasar por ninguna regla. Arreglado acotando `productId` a `z.string().min(1).max(200).regex(/^[A-Za-z0-9_-]+$/)` en `recalculateRatingRequestSchema`, con un test de regresión que prueba exactamente ese payload. Se verificó también si la misma técnica afectaba a los servicios del cliente (`saveReview`/`deleteReview`/`getProductReviews`, que arman el path con la forma multi-argumento `doc(db, 'products', productId, 'reviews', uid)`) — confirmado con Context7 que esa forma también re-parte el string, pero ahí no es explotable: `firestore.rules` matchea por profundidad exacta (`match /products/{productId} { match /reviews/{reviewId} { ... } }`, 4 segmentos), así que cualquier `/` embebido en `productId` produce un path de otra profundidad que cae en el `match /{document=**} { allow read, write: if false; }` del techo del archivo. La Function, al usar el Admin SDK, no tenía ese backstop — por eso el hallazgo era real solo ahí.

**Reglas de Firestore, con la validación completa (corregida en el auto-review del contrato antes de implementar, no encontrada después):** además de tipo/rango/largo de `rating`/`comment`/`displayName`, `create` exige `createdAt == request.time` y `update` exige `createdAt == resource.data.createdAt` (inmutable) + `updatedAt == request.time` siempre — sin esto, un cliente fuera de la app podía fabricar fechas, violando la regla ABSOLUTA de timestamps del servidor. Las condiciones repetidas entre `create`/`update` se factorizaron en `function hasValidReviewFields()` (hallazgo de `/simplify`, confirmado que Firestore rules soporta funciones con acceso implícito a `request`/`resource`).

**`Modal` de la slice 8 tuvo su primer reuso real, vía un `ConfirmActionModal` nuevo en `components/ui/`** (hallazgo de `/simplify`, señalado independientemente por los ángulos de reuse y simplificación): `CancelOrderModal` (de la 8) y `DeleteReviewModal` eran casi idénticos —mismo estado, mismo layout, mismo botón "Volver" carácter por carácter— y ahora los dos son wrappers finos sobre `ConfirmActionModal` (`title`, `body`, `confirmLabel`, `onConfirm: () => Promise<void>`, lanza para mostrar error sin cerrar). `DestructiveTriggerButton` (también nuevo) unifica el botón que abre una confirmación destructiva, reusado por `OrderDetailPage` ("Cancelar orden") y esta slice ("Borrar mi reseña"). `useFieldDescribedBy` extrae el cálculo de `aria-describedby` que `TextField` y el `Textarea` nuevo compartían casi entero. `formatOrderDate.ts`/`formatReviewDate.ts` (duplicados byte a byte) se consolidaron en `src/lib/formatDate.ts`. `useProductReviews` dejó de anidarse dentro del fetch del producto — ahora se llama en paralelo (mismo patrón `id: string | undefined` que `useProduct`/`useOrder`), así las dos consultas de Firestore salen a la vez en vez de encadenarse. `recalculateProductRating` se sacó de `ReviewForm`/`DeleteReviewModal` y se centralizó en `ProductReviewsPage`, junto al `retry()` que ya vive ahí.

**`RatingInput` usa `Controller` de react-hook-form, no `register`, por un bug real confirmado con un test aislado antes de llegar a producción:** el auto-chequeo nativo de radios agrupados de RHF compara `defaultValues` contra el atributo `value` del DOM, y como el campo es `number` (para matchear `z.number().int().min(1).max(5)`) contra un `value` que el navegador siempre trata como string, ningún radio quedaba marcado al precargar una reseña existente — reproducido con un test mínimo (`defaultChecked` en el DOM en `true`, `.checked` real en `false`) antes de cambiar de enfoque. `Controller` con `checked={field.value === value}`/`onChange` explícito evita por completo ese mecanismo. La calificación se muestra sin glifos (`RatingDisplay`, cinco segmentos contorno/relleno reusando el vocabulario de `OrderStatusBadge`, con el número siempre en texto) — coherente con la convención de este sistema de "cero íconos".

Verificado en vivo con `vercel dev` + el emulador seedeado, con dos cuentas nuevas: crear una reseña (agregado actualizado al instante, sin recargar), editarla (`createdAt` preservado, `updatedAt` refrescado — confirmado en Firestore vía REST), borrarla (documento eliminado, `ratingAverage`/`ratingCount` del producto vueltos a 0 vía la Function, confirmado vía REST), un segundo usuario ve la reseña del primero en la lista pero no puede editarla/borrarla ni ve su propio formulario precargado con datos ajenos (F8.3), dos reseñas de dos usuarios distintos coexisten (promedio calculado correctamente), estado vacío invitando a la primera reseña (F8.6), anónimo ve la lista pero no el formulario con un link a `/login` que vuelve exactamente a esta página tras loguearse (reusa el mecanismo de F13.1 de la slice 5), Escape en el modal de borrado cierra y devuelve el foco al disparador, mobile 320px y tema claro, desktop y tema oscuro, sin errores de consola en ningún paso.

**Slice 10 · Panel de administración · productos (F9)**, rama `feature/admin-products`: primer panel de administración real, y primera vez que una Vercel Function de este proyecto distingue por rol y no solo por autenticación.

**Decisión de arquitectura, documentada en [ADR 0008](docs/adr/0008-escritura-de-productos-via-vercel-function-por-complejidad-de-schema.md), sin reusar el razonamiento del ADR 0007 aunque el mecanismo (Function + Admin SDK) sea el mismo**: a diferencia de las órdenes (invariante cruzado entre documentos), acá el problema es la riqueza de un solo documento — `specs[]` es un array de 1 a 12 objetos, y Firestore rules no tiene iteración real sobre arrays de objetos, así que validarlo ahí exigiría reglas frágiles repetidas a mano por índice. Tres Functions nuevas (`api/admin/products/{create,update,delete}.ts`) con el Admin SDK; `products/{productId}` sigue sin ninguna regla de escritura de cliente.

**`verifyRequestToken` gana un campo `role`** (`'customer'|'admin'`, leído del propio custom claim del token ya verificado, sin ninguna lectura extra a Firestore — cambio aditivo, `create.ts`/`cancel.ts`/`recalculate.ts` no se tocan) y un helper nuevo `requireAdmin` (`api/_lib/`, extraído en `/simplify`) centraliza el chequeo de método+token+rol que las tres Functions repetían casi idéntico.

**`update.ts` es un único endpoint genérico de patch parcial** (`{productId, changes}`, `changes: productInputSchema.omit({nameLower:true}).partial()`), reusado por el formulario completo, la edición de celda de precio/stock, y retirar/reactivar (`{changes: {isActive: false}}`) — evita tres endpoints casi idénticos. No lee el documento antes de escribir (los valores son siempre absolutos, no deltas); si el producto no existe, el propio `.update()` del SDK lo rechaza (`GrpcStatus.NOT_FOUND`, duck-typed sin `instanceof`, mismo criterio que `getOrderById` en la slice 8) y se mapea a `PRODUCT_NOT_FOUND`. `create.ts` fija `nameLower`/`isActive: true`/los cuatro contadores en 0/`createdAt`+`updatedAt` con `FieldValue.serverTimestamp()` — ninguno confiado del cliente — y usa `.create()` en vez de `.set()` para que una colisión de UUID (astronómicamente improbable) falle ruidoso en vez de pisar en silencio. `delete.ts` exige `orderCount === 0 && ratingCount === 0` (ADR 0005) antes de borrar de verdad, sin transacción (un solo operador, frecuencia baja, decisión documentada en el propio ADR 0008). `productId` se valida en las tres con `productIdSchema` compartido (mismo regex cerrado de la slice 9, ahora con tres consumidores — retrofiteado también en `recalculateRatingRequestSchema`), aplicando la lección de la slice 9 sobre inyección de path en el Admin SDK desde el primer commit, no encontrándola de nuevo en `/security-review`.

**`scripts/grant-admin.ts` nuevo**, el prerrequisito que faltaba desde la Planificación: recibe email o uid por argumento, escribe `users/{uid}.role = 'admin'` en Firestore (fuente de verdad) y llama a `setCustomUserClaims` (espejo) — nunca un endpoint HTTP. Reusa `adminAuth`/`adminDb` de `api/_lib/firebaseAdmin.ts` en vez de reinventar la inicialización; `npm run grant-admin -- <email>` carga `.env` con `tsx --env-file`.

**Superficie construida contra el brief ya escrito en la Planificación** (`.impeccable/surfaces/src-routes-admin-adminlayout-tsx.md`): `AdminProductsPage` (`/admin/products`) trae el catálogo completo sin filtrar por `isActive` (a diferencia del catálogo público — un producto retirado tiene que seguir visible acá para poder reactivarlo) y filtra por categoría/nombre en el cliente, sin debounce (a diferencia del catálogo público: sin costo de red que debounciar una vez que la lista completa ya está en memoria). Precio y stock editables en la celda (`EditableNumberCell`, revierte al valor anterior y explica el error si falla, `key={product.price}` para resincronizar ante cambios externos sin lógica manual y sin perder el foco al guardar). Retirar/reactivar es un click directo sin modal (es reversible, no es "destructiva e irreversible" según la regla ABSOLUTA del proyecto); "Eliminar definitivamente" sí pasa por `ConfirmActionModal` (slice 9), y solo aparece si `orderCount === 0 && ratingCount === 0`; si no, la pantalla explica el motivo con palabras (ventas, reseñas, o ambas). Formulario único en `/admin/products/new` y `/admin/products/:id/edit` (`AdminProductForm`), con `specs[]` vía el primer `useFieldArray` de este proyecto. Tabla real en desktop, lista apilada en mobile sin scroll horizontal (cada `<td>` lleva su propia etiqueta inline oculta en desktop).

**`/simplify` (4 ángulos) encontró un bug real de altitud**: `EditableNumberCell` guardaba el valor confirmado en estado propio que nunca se resincronizaba ante un cambio externo (el refetch que dispara otra fila al retirar/eliminar), y el guardado de precio/stock nunca disparaba `onMutated()` a diferencia de retirar/eliminar — corregido keyeando la celda por su propio `value` y haciendo que las cuatro mutaciones de fila sigan el mismo patrón de confirmar y refrescar. Además: `requireAdmin` extraído (3 Functions casi idénticas), `productSchema.pick(...)` hoisteado a nivel de módulo en `delete.ts` (se reconstruía en cada request), y `BUTTON_CLASSES`/`STATUS_BADGE_BASE_CLASSES`/`FILTER_CHIP_CLASSES` extraídos y compartidos con `Button`/`OrderStatusBadge`/`CatalogFilters` respectivamente. Descartado con motivo: debounce en el buscador admin (sin costo de red que justifique la demora, y contradice la tesis del brief de "dos interacciones, no seis"), sacar el `throw`/`catch` de `delete.ts` (ya es el patrón establecido en `recalculate.ts`/`cancel.ts` para varios chequeos de negocio dentro de un mismo try), sacar la rama `!id` de `useAdminProduct` (espeja `useProduct` a propósito, para que el hook sea seguro sin importar la disciplina del caller).

**`/security-review` no encontró hallazgos** de confianza suficiente. Un candidato (falta de transacción en el guard de `delete.ts`) quedó filtrado en 2/10: las órdenes guardan un snapshot, no una referencia viva, así que el peor caso no compromete datos de terceros, y es una decisión ya documentada en el ADR 0008.

**Pasada de diseño con Impeccable (`polish`)**: un hallazgo real de contraste — los labels de columna usaban `/50` de opacidad para texto, un valor documentado en este sistema solo como excepción de *borde* (nunca de texto), medido en ≈3.5:1, por debajo del piso AA de 4.5:1; corregido a `/60` (≈4.8:1, el mismo valor ya verificado del chip de categoría inactivo), extraído a `TABLE_LABEL_CLASSES`. De paso se completó `DESIGN.md`, que saltaba directo de la slice 7a a esta sin documentar las slices 8 y 9 — diez entradas de componentes nuevas (`Modal`, `ConfirmActionModal`, `OrderStatusBadge`, `OrdersPage`, `OrderDetailPage`, `RatingDisplay`, `RatingInput`, `Textarea`, `ReviewForm`, `ProductReviewsPage`) derivadas del código real, no de memoria.

**Verificado en vivo** con `vercel dev` + el emulador seedeado, con `grant-admin.ts` promoviendo una cuenta de prueba real: editar precio en la celda (confirmado en Firestore vía REST, `updatedAt` refrescado con timestamp de servidor), retirar (desaparece del catálogo público, confirmado en una pestaña separada, sigue en la tabla admin como "RETIRADO"), reactivar, eliminar definitivamente (modal nombra el producto, foco inicial en "Volver", la fila desaparece solo tras confirmar el backend), crear y editar un producto completo (formulario pre-cargado correctamente), filtro/búsqueda (distingue "sin productos" de "sin resultados"), guard de acceso (anónimo → `/login`, customer → `AccessDeniedState`; las tres Functions ya cubiertas por tests contra el emulador real con `role: customer` → 403 `FORBIDDEN`), mobile 320px y desktop, tema claro y oscuro, sin errores de consola.

**Deploy de preview falló al abrir el PR, ajeno al código de la slice pero destapado por ella**: Vercel rechazó el build con `exceeded_serverless_functions_per_deployment` — el plan Hobby tope a 12 Serverless Functions por deployment, y Vercel trata cualquier archivo bajo `api/` que no empiece con `_` o `.` como una función propia, **incluidos los `*.test.ts`** (solo `api/_lib/` está exento, por el prefijo `_`). Antes de esta slice había 4 endpoints reales + 4 tests = 8, bajo el límite por casualidad; los 3 endpoints y 3 tests nuevos de esta slice lo llevaron a 14. Arreglado con `.vercelignore` nuevo (`api/**/*.test.ts`) — excluye los tests del bundle que sube a Vercel sin afectar `npm test`/`npm run test:functions`, que los corren localmente. Confirmado con Context7 contra el propio código fuente de Vercel (`fs-detectors`) qué prefijos exceptúa la detección de funciones, en vez de asumirlo.

**Slice 11 · Subida de imágenes a S3 (F10)**, rama `feature/product-image-upload`: reemplaza el campo de texto libre "URL de la imagen" de `AdminProductForm` (slice 10) por una subida real — el admin elige un archivo, el frontend pide una URL prefirmada a una Vercel Function nueva, sube directo a S3 (ADR 0003, ya decidido en la Planificación), y el `imageUrl` del producto pasa a ser la URL pública real.

**Decisión técnica verificada con Context7 antes de escribir código, que corrigió una suposición de la Planificación**: la lista de dependencias original preveía `@aws-sdk/s3-request-presigner` (presigned PUT). Al verificar en la documentación del SDK v3 cómo atar un límite de tamaño a la firma, se confirmó que `content-length-range` **solo existe en `createPresignedPost`** (presigned POST con policy), no en `getSignedUrl`+`PutObjectCommand`. Se lo planteé a Hernán como una decisión real (PUT+validación en la función vs. cambiar a POST, que hubiera exigido editar a mano el CORS del bucket ya configurado por `scripts/setup-infra.sh`, limitado a `AllowedMethods: [PUT, GET]`) — **eligió mantener PUT**. El tamaño máximo (5 MB) queda validado por la función antes de firmar, no garantizado por S3 mismo; el tipo sí queda 100% garantizado (`Content-Type` firmado vía `signableHeaders`). Aceptable porque el endpoint es admin-only.

**`api/admin/uploads/presign.ts`** (nuevo): gateado por `requireAdmin` (mismo helper de slice 10, ver abajo), valida `{contentType, fileSize}` con Zod contra una whitelist cerrada de 3 tipos (`image/jpeg`, `image/png`, `image/webp` — **sin SVG a propósito**, vector de XSS conocido) y un tope de 5 MB, genera `key = products/{uuid}.{ext}` (nunca el nombre original — `checklist-auth`), firma con `expiresIn: 300` (5 min). `api/_lib/s3Env.ts`/`s3Client.ts` nuevos, mismo patrón que `env.ts`/`firebaseAdmin.ts` pero con su propio schema Zod separado (no comparten validación con las vars de Firebase Admin — evita que un test o módulo que solo necesita S3 tenga que proveer también credenciales falsas de Firebase, y viceversa).

**`requireAdmin` generalizado**: dejó de construir un `ProductError` a mano y pasó a recibir un `makeError(code, message)` + `respond` por parámetro, para que `uploads` (segundo dominio real que necesita el gate de admin) lo reuse sin acoplarse a `ProductError`. Iteración intermedia (un objeto `RequireAdminErrors` con los 3 errores pre-construidos) fue señalada por dos agentes de `/simplify` (simplification y efficiency) como sobre-construida — reconstruía 3 `Error` en cada request cuando a lo sumo se usa 1 — y se reemplazó por el diseño final de un solo `makeError`, sin tipo genérico de "bolsa de errores". Toca los 3 endpoints de productos (mismos mensajes, mismo comportamiento, cambio mecánico).

**Bug real encontrado por `/simplify` (altitude), corregido antes de commitear**: la subida de imagen y el submit del formulario eran dos máquinas de estado sin comunicación — `useImageUpload` vivía dentro de `ImageUploadField` y solo deshabilitaba su propio input; el `isBusy` de `AdminProductForm` no sabía nada de la subida en curso, así que un admin podía apretar "Crear producto"/"Guardar cambios" mientras la imagen todavía se subía y el formulario se enviaba con el `imageUrl` viejo, en silencio. Arreglado moviendo `useImageUpload` a `AdminProductForm` (el dueño real del gate de submit) e invirtiendo `ImageUploadField` a puramente presentacional (`state`/`onFileSelected` por props, sin hook propio) — `isBusy` ahora incluye `requesting-url`/`uploading`, y el mismo `<fieldset disabled={isBusy}>` que ya cubre el resto del formulario cubre también la subida. Test de regresión nuevo (`AdminProductForm.test.tsx`) que verifica el botón deshabilitado durante la subida.

Otros hallazgos de `/simplify` aplicados: `FIELD_LABEL_CLASSES` (duplicado entre `AdminProductForm` e `ImageUploadField`) extraído a un archivo compartido — no reexportado de un componente al otro, porque eso hubiera creado un import circular entre los dos; el campo `step` del estado de error (`ImageUploadState`) sacado por no tener ningún consumidor real (`ImageUploadField` solo lee `message`); progreso de subida redondeado a múltiplos de 5% para no re-renderizar en cada tick del XHR. Descartado con motivo: unificar `uploadErrors.ts` con `productErrors.ts`/`orderErrors.ts`/`reviewErrors.ts` en un factory genérico — tocaría archivos ya mergeados de slices anteriores por una ganancia cosmética; el patrón de un módulo de errores por dominio ya se repitió deliberadamente 3 veces en este proyecto.

**`/security-review` no encontró hallazgos.** Verificó explícitamente: la key de S3 no admite inyección de path (UUID generado por el servidor + extensión de un mapeo cerrado, sin input del cliente en ninguna parte de la key), la generalización de `requireAdmin` no abre ningún bypass de rol (mismo orden de chequeos, cada rama corta con `return null`), la presigned URL queda criptográficamente acotada a esa key exacta (no permite sobreescribir otro objeto), y las credenciales de AWS quedan tan aisladas de `src/` como las de Firebase Admin.

**Pasada de diseño con Impeccable (`polish`)**: un hallazgo menor — el input de archivo (primer `<input type="file">` de este proyecto) no tenía tratamiento de hover/focus-visible en su `::file-selector-button`, a diferencia de `Button` y el resto de los controles interactivos del sistema; corregido reusando el mismo lenguaje (`border-current`, acento de tema, `focus-visible:outline-2`). Documentado en `DESIGN.md`: entrada nueva para `ImageUploadField` (primer file input, primera barra de progreso de este proyecto) y un Do nuevo sobre levantar el estado de un sub-flujo async al padre cuando el gate de submit del padre necesita conocerlo.

**Gotcha de entorno de desarrollo, variante nueva del ya documentado en la slice 7b**: esta vez `vercel.json` tiene `"framework": "vite"`, así que `vercel dev` detecta el framework y corre su propio `vite --port $PORT` como "Dev Command" en el mismo puerto que sirve las Functions — visitar ese puerto directo da una página en blanco con 500s (mismo choque entre el rewrite SPA y los requests internos de Vite ya documentado). La solución es la misma de siempre pero con el matiz de *a qué puerto no hay que entrar*: `vercel dev` sigue sirviendo `/api/*` correctamente en su puerto (confirmado con `curl /api/health` → 200) aunque su copia interna del frontend esté rota; el navegador nunca visita ese puerto, visita el `npm run dev` real (Vite en 5173, que proxea `/api` al puerto de `vercel dev`).

**Verificado en vivo** con `vercel dev` + `npm run dev` + el emulador seedeado, con `grant-admin.ts` promoviendo una cuenta de prueba real y un bucket S3 real (creado antes de esta sesión vía `scripts/setup-infra.sh`, con la bucket policy y el CORS ya cargados): crear un producto subiendo una imagen real (PNG de prueba), confirmado el objeto público en S3 vía `curl` (200, `Content-Type: image/png`, exactamente el tipo firmado), editar el mismo producto y ver la imagen ya subida como preview sin forzar una re-subida (regla de formularios de edición del checklist), rechazo de un archivo `.txt` ("Formato no soportado. Usá JPG, PNG o WEBP.") y de un archivo de 6 MB ("La imagen no puede superar los 5 MB.") — los dos sin llegar a pedir la URL prefirmada, mensajes distintos y claros, mobile 320px y desktop, tema claro y oscuro, sin errores de consola.

**Slice 12 · Panel de administración · órdenes (F11)**, rama `feature/admin-orders`: segunda sección del panel admin — ver todas las órdenes (no solo las propias), filtrar por estado, y cambiar el estado de una orden respetando el grafo de transiciones del dominio, con cancelación devolviendo stock y contadores en la misma transacción.

**Decisión real, planteada y confirmada antes de escribir código: `orderCount`/`unitsSold` ahora bajan al cancelar una orden, en los dos caminos (cliente y admin).** F12.3 (dashboard, la próxima extra) exige explícitamente que `unitsSold` baje al cancelar, para que una orden cancelada no cuente como venta en el ranking — pero la slice 8 había decidido lo contrario ("nunca se revierten"), citando el guard de hard-delete de ADR 0005. Le planteé la inconsistencia a Hernán con tres opciones (solo `unitsSold`, los dos campos, o postergar) — **eligió que bajen los dos**. Consecuencia asumida: `orderCount` pasa de significar "cuántas veces se pidió alguna vez" a "cuántas órdenes activas lo referencian ahora", y el guard de ADR 0005 (`orderCount === 0 && ratingCount === 0`) cambia de comportamiento en la práctica — un producto cuya única orden se canceló vuelve a ser hard-deletable. El ADR no se edita (nunca se tocó ese archivo); esta es la bitácora de la decisión. Verificado en vivo: tras cancelar la única orden de un producto, "Eliminar definitivamente" volvió a aparecer en `/admin/products` donde antes decía "Tiene 1 ventas registradas".

**Grafo de transiciones nuevo en `shared/schemas/order.ts`** (`ORDER_STATUS_TRANSITIONS` + `isValidOrderStatusTransition`), fuente única consumida tanto por el cliente (qué botones renderiza `AdminOrderRow`) como por la Function nueva (qué acepta, revalidando siempre contra el `status` leído fresco de Firestore dentro de la transacción, nunca lo que mande el cliente): `pending → processing | cancelled`, `processing → completed | cancelled`, `completed`/`cancelled` terminales. F11.6 (no cancelar dos veces) queda cubierto gratis por el propio grafo — un segundo intento de cancelar lee `status: 'cancelled'` y el chequeo lo rechaza, sin necesitar una clave de idempotencia como la de crear una orden.

**`api/admin/orders/update-status.ts`** (nuevo): gateado por `requireAdmin` (tercer dominio que lo reusa, después de productos y uploads). La restitución de stock/contadores al cancelar se extrajo a un helper compartido, `api/_lib/orderCancellation.ts` (`cancelOrderAndRestoreStock`), usado tanto por este endpoint como por `api/orders/cancel.ts` (slice 8, retrofitado) — un solo lugar decide cuánto restar: `orderCount -1` por producto (una vez, sin importar la cantidad, espejando el `+1` de `create.ts`), `unitsSold -cantidad` por producto. El `/simplify` de altitude marcó que esto cambia comportamiento ya documentado del endpoint de slice 8 — señal correcta, pero la premisa de "nadie lo decidió" no aplicaba: es exactamente la decisión de arriba, confirmada antes de tocar el código.

**Hueco real encontrado en la revisión del propio contrato, antes de escribir código: `orderId` no tenía charset cerrado.** `cancelOrderRequestSchema.orderId` (slice 8, ya mergeado) era `z.string().min(1)` sin regex, interpolado directo en `adminDb.doc(\`orders/${orderId}\`)` — la misma clase de vulnerabilidad que encontró `/security-review` en la slice 9 (inyección de path vía Admin SDK). Corregido con `orderIdSchema = z.uuid()` en `shared/schemas/order.ts`, más estricto que el regex genérico de `productIdSchema`: toda orden real nace con `orderRequestId = crypto.randomUUID()` (`create.ts`, sin otra vía de creación), así que `z.uuid()` es más preciso que copiar el charset abierto de productos, que existe por una razón distinta (soportar los IDs auto-generados de Firestore de `scripts/seed.ts`). Aplicado retroactivo a `cancelOrderRequestSchema` también.

**Otro hueco real, encontrado en la misma revisión: `ORDER_ERROR_CODES` no tenía `FORBIDDEN`.** Nunca hizo falta hasta ahora — ni `create.ts` ni `cancel.ts` usan `requireAdmin`, se gatean por ownership. `requireAdmin` exige que el código de error del dominio incluya `FORBIDDEN` (403); se sumó a `ORDER_ERROR_CODES` y a `ORDER_ERROR_HTTP_STATUS`.

**`firestore.rules`**: la regla de lectura de `orders/{orderId}` gana `|| request.auth.token.role == 'admin'`, sin agregar ninguna escritura de cliente (siguen siendo 100% Admin SDK). El caso nuevo — el admin listando *todas* las órdenes sin ningún filtro de `userId` — no estaba probado en este proyecto (la slice 8 solo había probado `list` con ownership). Verificado con Context7 antes de escribir la regla: una condición en OR que no depende de `resource.data` (como el claim de admin) se puede probar válida para cualquier documento posible sin necesitar un filtro que la acote, a diferencia de una condición que sí lee `resource.data` (ahí sí aplica "Rules are not filters"). Confirmado empíricamente con 5 tests nuevos contra el emulador real, incluida la query sin filtro. Nuevo índice compuesto `status + createdAt` en `firestore.indexes.json`.

**`/simplify` (4 ángulos) encontró tres duplicaciones reales, aplicadas:** `AdminCancelOrderModal` (nuevo) era casi idéntico a `CancelOrderModal` (slice 8) — colapsados en uno solo, `CancelOrderModal` ahora recibe la acción de cancelar como prop (`onCancel: () => Promise<Result>`) en vez de construirla con `user` internamente, así el customer (`cancelOrder`) y el admin (`updateOrderStatus(..., 'cancelled')`) comparten el mismo componente y el mismo test. `AdminOrdersSkeleton` era byte a byte igual a `AdminProductsSkeleton` (slice 10) — unificados en `src/components/ui/TableSkeleton.tsx` (`rows` opcional). El `className` de los dos `NavLink` de `AdminLayout` repetía el mismo ternario — extraído a `navLinkClassName` + un array `ADMIN_NAV_LINKS`. De paso, `TABLE_LABEL_CLASSES` se movió de `features/admin/products/constants/` a `features/admin/constants/`, ahora que dos sub-secciones admin la comparten. Descartado con motivo: optimizar el refetch completo de la lista tras cambiar una fila (es el mismo patrón ya establecido en `AdminProductsPage` desde la slice 10, y tocarlo solo acá crearía una inconsistencia); generalizar `parseOrderStatusParam` junto con `parseCategoryParam`/`parseSearchTermParam`/`parsePageParam` en un helper genérico (mismo criterio de siempre: no tocar archivos ya mergeados por una ganancia cosmética en un idioma de 3 líneas).

**`/security-review` no encontró hallazgos.** Verificó explícitamente: `requireAdmin` en el endpoint nuevo sigue el mismo orden de chequeos que los demás, `orderId` cerrado con `z.uuid()` en los dos endpoints, el grafo de transición se revalida server-side contra el `status` leído de Firestore (nunca el del cliente), y la regla nueva de Firestore solo amplía lectura para el claim de admin sin debilitar la rama de ownership.

**Pasada de diseño con Impeccable (`polish`)**: sin hallazgos. Todo lo construido reusa tokens y componentes ya verificados (`FILTER_CHIP_CLASSES`, `TABLE_LABEL_CLASSES`, `tabular-nums`, el lenguaje de texto-subrayado ya usado por `DestructiveTriggerButton`/"Retirar-Reactivar") y el nav de `AdminLayout` — que `DESIGN.md` ya dejaba anotado como pendiente "hasta que una segunda sección exista" — es exactamente esa condición cumplida. Documentado en `DESIGN.md`.

**Verificado en vivo** con el emulador seedeado + `vercel dev` + `npm run dev`, un customer real comprando (orden `pending` real, no seedeada) y una cuenta promovida a admin con `grant-admin.ts`: el admin ve la orden de otro usuario (F11.1), filtro por estado con `?status=` en la URL y "sin resultados" distinto de "todavía no hay órdenes" (F11.2), `pending → processing` sin tocar stock, `processing → cancelled` con confirmación (modal, foco inicial en "Volver") restituyendo stock y habilitando "Eliminar definitivamente" en `/admin/products`, estado terminal sin ningún botón (F11.4), un customer entrando a `/admin/orders` recibe `AccessDeniedState` (F11.7) — todo en mobile 320px, desktop, tema claro y oscuro, sin errores de consola en ningún paso. Un gotcha de testing propio, no del producto: los clicks simulados de Chrome DevTools MCP no dispararon el submit de un par de formularios (checkout, login) — se resolvió llamando `.click()` directo sobre el elemento vía `evaluate_script` en esos casos puntuales, sin que fuera un bug real de la app.

**Slice 13 · Dashboard de analytics (F12)** *(extra)*, rama `feature/admin-analytics`: tercera y última sección del panel admin — ingresos totales, cantidad de órdenes y un ranking de los 5 productos más vendidos, todo con agregación de Firestore, sin traer documentos de más.

**Decisión de estructura, resolviendo la pregunta que el brief de admin había dejado explícitamente abierta** ("si el dashboard de analytics es una sección más del panel o su propia pantalla de entrada"): ruta propia `/admin/analytics`, tercer link en `ADMIN_NAV_LINKS` de `AdminLayout` — mismo patrón que productos y órdenes, sin código nuevo en el guard ni en el layout más allá de la entrada del array.

**Sin Vercel Function nueva: lectura directa desde el cliente, mismo criterio que `getAllOrders`/`getOrders`.** No hay ningún invariante cruzado que proteger acá, solo lectura. Dos queries en total: `getOrdersSummary()` hace una sola llamada a `getAggregateFromServer` sobre `orders` filtradas por `status != 'cancelled'`, con `{ totalRevenue: sum('total'), totalOrders: count() }` — un viaje de red, cero documentos transferidos (F12.2). `getTopSellingProducts()` hace un `getDocs` con `where('unitsSold', '>', 0), orderBy('unitsSold', 'desc'), limit(5)` sobre `products` — el `where` evita que productos nunca vendidos aparezcan como "más vendidos" con 0 unidades, sin necesitar índice compuesto nuevo (desigualdad + orden sobre el mismo campo, cubierto por el índice automático de campo único). F12.3 ya estaba resuelto de antemano: `unitsSold` baja al cancelar desde la decisión de la slice 12, así que una orden cancelada nunca infla el ranking ni los ingresos — verificado en vivo, no solo en teoría (ver abajo).

**Contrato revisado tres veces antes de escribir código** (dos rondas a pedido explícito más una tercera con informe de conveniencia), con hallazgos reales en cada una: (1) el resultado de `sum()`/`count()` de Firestore sobre una colección vacía no está documentado como `0` garantizado — se valida con `z.number().nullable().transform(v => v ?? 0)` en vez de asumirlo, mismo criterio que `ratingAverage` en la slice 9. (2) sumar valores ya redondeados en el agregado del servidor puede reintroducir error de punto flotante — el resultado se redondea con `roundToCents()` (ya existente en `shared/schemas/order.ts`). (3) el nombre `orderCount` para el total de la tienda colisionaba con el campo `Product.orderCount` (contador por producto, significado completamente distinto) — renombrado a `totalOrders` antes de escribir una sola línea de código. (4) verificado con Context7 y confirmado empíricamente que las security rules de Firestore evalúan una consulta de agregación exactamente igual que una consulta de documentos — dos tests nuevos contra el emulador real (`tests/rules/firestore.rules.test.ts`): un customer con `getAggregateFromServer` sin filtro de `userId` sobre `orders` falla, un admin con la misma consulta tiene éxito. Sin cambios a `firestore.rules` ni a `firestore.indexes.json`: la rama de admin ya escrita en la slice 12 alcanza.

**`/simplify` (4 ángulos) encontró, de forma independiente y repetida, dos hallazgos reales:** el skeleton de carga estaba escrito inline en la página/sección en vez de como componente propio — rompía una convención muy establecida en este proyecto (`TableSkeleton`, `OrderDetailSkeleton`, `ProductCardSkeleton`, etc., nunca un `<div>` de pulso suelto) — extraídos `AnalyticsSummaryTilesSkeleton`/`TopSellingProductsChartSkeleton`, compartiendo la clase del contenedor real vía una constante exportada. Un `analyticsSummarySchema` de Zod que nunca se usaba en runtime (solo para derivar el tipo, mientras la validación real corría por un segundo schema sin los mismos chequeos) — colapsado en un solo schema con `.transform()` que valida y da forma al resultado a la vez, cerrando el hueco que ya había señalado el propio proyecto como anti-patrón ("Asumir que `z.infer` valida en runtime"). Además: `useCallback` innecesario en los dos hooks (los servicios son funciones de módulo ya estables, sin argumentos, no hacía falta envolverlas) y un guard de lista vacía agregado en `TopSellingProductsSection` como defensa en profundidad — el gate de estado vacío de página completa (F12.4) descansa en un invariante matemático real (`totalOrders === 0` para no-canceladas implica `unitsSold === 0` en todo el catálogo, porque cada cancelación deshace exactamente lo que sumó su propia orden) pero nada en el código lo garantiza entre dos colecciones leídas por separado, así que la sección igual explica con texto si el ranking viniera vacío por algún motivo futuro no previsto.

**`/security-review` no encontró hallazgos.** Slice de solo lectura: sin Function nueva, sin input de cliente en ninguna query (los dos servicios no reciben argumentos), autorización 100% delegada a reglas ya existentes y ya probadas (`orders` para el agregado, la lectura pública ya existente de `products` para el ranking — no expone nada que no fuera ya público desde la slice 7b).

**Pasada de diseño con Impeccable (`polish`), un hallazgo real corregido antes de verificar en vivo:** el primer diseño de `AnalyticsSummaryTiles` era dos celdas con borde propio — exactamente el "hero-metric template" (número grande + etiqueta chica dentro de una tarjeta) que el checklist de diseño rechaza por defecto, y que el propio brief de admin ya había descartado explícitamente en su THESIS ("Rechaza el panel de administración que imita un dashboard de producto —tarjetas de métricas, gráficos decorativos, aire de sobra—"). Corregido reusando el idioma que ya existe en `OrderDetailPage` para sus totales: un solo `<dl>` que fluye, separado del contenido de arriba por un hairline (`border-t`), con los dos números en columna en mobile y divididos por un hairline vertical desde `sm:` — nunca cajas. `recharts` (nueva dependencia, primer gráfico de este proyecto) resuelve todos sus colores con `currentColor` sobre un contenedor `text-ink dark:text-bone`, así que hereda el tema sin ningún token de color nuevo; cada barra muestra su valor con `LabelList` directo sobre el gráfico (F12.5, ninguna serie depende del color) y la animación de entrada queda desactivada del todo (`isAnimationActive={false}`), coherente con que esta superficie nunca usa motion decorativo. Documentado en `DESIGN.md`.

**Verificado en vivo** con el emulador seedeado + `vercel dev` + `npm run dev`, una cuenta real promovida a admin con `grant-admin.ts` que además compró como cliente (dos compras reales, no seedeadas): el dashboard muestra ingresos y cantidad de órdenes correctos, el ranking lista los productos comprados con su unidad vendida cada uno, y **F12.3 se verificó de punta a punta, no solo por inspección de código**: cancelar la única orden existente devolvió el dashboard al estado vacío completo (F12.4, sin gráficos ni ceros engañosos), confirmando que la cancelación de la slice 12 se refleja correctamente acá. Todo en mobile 320px, desktop, tema claro y oscuro, sin errores de consola en ningún paso.

**Slice 14 · Landing (F14)**, rama `feature/landing`: última slice del plan original — cierra el criterio de que el primer viewport comunique qué es CLACK sin hacer scroll, con una acción primaria visible hacia el catálogo.

**Decisión de estructura, confirmada con Hernán antes de escribir el contrato**: la landing no es una ruta nueva. `CatalogPage` (sigue en `/`) gana una sección de "pieza destacada" arriba de la grilla ya existente — coincide con el propio brief de superficie, cuyo FIRST VIEWPORT ya describía header + riel + pieza destacada + grilla como una sola pantalla, y con la nota que la slice 1 dejó escrita ("la pieza destacada del hero... van con la landing en la slice 14"). Sin campo `featured` nuevo: la pieza se elige por el producto activo más reciente (`getFeaturedProduct`, `where('isActive','==',true), orderBy('createdAt','desc'), limit(1)`), reusando el índice compuesto `isActive + createdAt` que ya existe desde la slice 2 — sin índice nuevo, sin Function nueva (lectura directa, mismo criterio que el resto del catálogo público).

**Contrato revisado tres veces a pedido explícito, con hallazgos reales en cada ronda.** (1) La pieza destacada se duplicaba en la grilla de abajo (siendo el producto más reciente, es siempre el primer resultado del orden por defecto) — el propio brief lo señalaba con sus palabras ("el **resto** de la sala"): se excluye de la grilla solo cuando no hay filtro/búsqueda activa y es la página 1 (con filtro o búsqueda sí puede reaparecer — ahí es un resultado real, no un duplicado); narrowing seguro de `docs[0]` bajo `noUncheckedIndexedAccess` sin non-null assertions; el nombre de la pieza destacada va como `<h2>`, no `<h1>` (la página no tiene headline propio, el wordmark de `PublicLayout` sigue siendo el único `<h1>`); ancla `id="catalogo"` para el link secundario. (2) Carrera real entre `useFeaturedProduct` y `useActiveProducts` (dos fetches independientes, si la grilla resuelve antes que el hero la exclusión no se aplica todavía) — **aceptada, no sincronizada**: acoplar las dos secciones para evitar un parpadeo de milisegundos rompería el principio ya establecido en la slice 13 de que dos secciones independientes nunca se bloquean entre sí. (3) Hueco real: nunca decidí si la tabla de specs (hasta 12 filas) iba en el hero — se excluye a propósito, mismo criterio que la slice 3 con "número de pieza y fecha de ingreso" del mismo brief, para no arriesgar el CTA fuera del primer viewport a 320px; ajuste de vocabulario ("Ver el resto de la sala", no "colección", para seguir la metáfora ya establecida de "la sala"); alternativa de ahorrar una lectura de Firestore derivando la pieza del primer resultado de la grilla, evaluada y rechazada por la misma razón del hallazgo (2) — acoplaría el hero al estado de filtro/página de la grilla.

**`/simplify` (4 ángulos) encontró hallazgos reales en tres de los cuatro:** duplicación byte a byte del frame de imagen entre `FeaturedProductHero`/su skeleton y `ProductDetailPage`/`ProductDetailSkeleton` — extraída `PRODUCT_IMAGE_FRAME_HEIGHT_CLASSES` (`src/features/products/constants/productImageFrame.ts`), compartida entre los cuatro en el momento de la extracción. Cadena de variables derivadas en `CatalogPage` innecesariamente compleja (`catalog.status === 'success'` chequeado dos veces, una indirección sin uso propio) — colapsada en `featuredIdToHide`/`isCatalogEmpty` con nombres que dicen qué protegen, más un chequeo de estado redundante (`featured.status === 'success' && featuredProduct`, cuando `featuredProduct` ya implica ese estado) simplificado. `fetchPriority="high"` sumado a la imagen del hero (candidata real a LCP, único `loading="eager"` de este proyecto). **Dos hallazgos evaluados y descartados con motivo, no aplicados:** evitar la lectura duplicada de Firestore reusando el primer resultado de la grilla (mismo motivo que el rechazo de la ronda 2 del contrato: acoplaría el hero a la grilla) y coordinar las dos queries en una sola para que la página 1 siempre muestre 8 tarjetas exactas en vez de 7 tras la exclusión (tocaría `getActiveProducts`/`useActiveProducts`, ya enviados desde la slice 2/4, para un problema sin impacto real — nada se pierde, el ítem excluido se ve en el hero, `hasNextPage` sigue siendo correcto para los datos reales).

**`/security-review` no encontró hallazgos.** Slice enteramente de lectura: sin Function nueva, sin input de cliente en ninguna query (ninguno de los dos servicios recibe argumentos), autorización 100% delegada a reglas ya públicas desde la slice 1 (`products`).

**Pasada de diseño con Impeccable (`polish`), un hallazgo real encontrado en vivo, no por inspección de código:** a 320×800, el botón "Agregar al carrito" quedaba fuera del primer viewport sin scroll (medido con precisión: su borde superior arrancaba en 786px de un viewport de 800px) — violando F14.1/F14.2 por un margen chico pero real. La causa: el frame de imagen del hero reusaba `min-h-96 md:min-h-144` de la cartela del detalle de producto (extraído en el propio `/simplify` de esta slice), que es un **piso**, no un techo — en la cartela eso está bien porque el producto es lo único en esa pantalla y puede crecer libremente; en el hero, que compite por presupuesto de viewport con el CTA que tiene que verse sin scroll, un piso no alcanza. Corregido dándole al hero su propia altura en mobile: `h-64` (256px, techo real, no piso) en la base, `md:h-auto md:min-h-144` (vuelve a piso) desde `md:` — desktop sin cambios, ya entraba todo. Verificado de nuevo con la misma medición exacta: el botón pasó a arrancar en 700px, con 66px de margen. Documentado en `DESIGN.md` como una divergencia deliberada entre dos componentes que antes compartían la misma proporción, justificada por una restricción de contenido real (F14.1/F14.2), no una inconsistencia a corregir.

**Verificado en vivo** con el emulador seedeado (18 productos) + `npm run dev` (sin `vercel dev`: esta slice no toca ninguna Vercel Function): el hero muestra la pieza más reciente con nombre, nota curatorial, precio y `AddToCartControl` funcional; la grilla de abajo la excluye en la vista por defecto (7 tarjetas en vez de 8, numeradas 01-07, sin repetir "Ridge"); buscar el nombre exacto de la pieza destacada la muestra duplicada a propósito (hero + resultado de búsqueda, comportamiento intencional de la ronda 1 del contrato); el link "Ver el resto de la sala" ancla correctamente a la grilla. Todo en mobile 320px, desktop, tema claro y oscuro, sin errores de consola (el único error de red observado, un 400 de refresh de token contra el emulador de Auth, es ajeno a esta slice — no se tocó nada de autenticación).

**Con esta slice se completa el plan original de 14 slices** (docs/spec.md, sección 6 / el plan de la Planificación).

- ✅ Paso 1 · Deploy de humo (PR #1)
- ✅ Paso 2 · Configuración: ESLint, Prettier, Tailwind v4 (PR #2)
- ✅ Paso 3 · CI en GitHub Actions, y registrado como required status check en `main` (PR #3)
- ✅ Paso 4 · Base de datos: Firebase Emulator Suite, `firestore.rules` cerradas, `scripts/seed.ts` (PR #5)
- ✅ Paso 5 · Variables y secrets (hecho vía `scripts/setup-infra.sh`)
- ✅ Paso 6 · Autenticación (cimiento): init de Firebase, `AuthContext`, `ProtectedRoute`/`AdminRoute` (PR #8)
- ✅ Paso 7 · Tres layouts base (`PublicLayout`, `PrivateLayout`, `AdminLayout`) con sus guards y el router real (PR #10)
- ✅ Paso 8 · Lista de slices (`docs/spec.md`, sección 6)
- ✅ Paso 9 · Verificación conjunta: producción responde (`/`, `/api/health`, deep link a `/account`), CI en verde, emulador con seed, los tres layouts probados con Chrome DevTools
